# Code Run — 2026-09-14 12:00 — 14-merge-provenance (all 8 tasks)
Branch: docs/fidelity-concision-brief   Base: 2b9a62b

## Tasks completed
| Sub-project | Task | Status | Commit | Files | Tests |
|---|---|---|---|---|---|
| 14-merge-provenance | T1 harden the MERGE paragraph | done | 2fcf703 | backend/care_plan/prompts/assemble_and_render.txt | — |
| 14-merge-provenance | T2 regression fixtures (three-way diagnosis merge + non-diagnosis merged item) | done | 19db7a2 | backend/tests/care_plan/test_pipeline_assembly.py | — |
| 14-merge-provenance | T3 prompt-text guard tests | done | d369797 | backend/tests/care_plan/test_pipeline_prompts.py | — |
| 14-merge-provenance | T4 compute and log `merge_candidate_signal` aggregate in `_verify_assembly` | done | c6b2b93 | backend/care_plan/pipeline.py (placed after the PRD 18 diagnosis block and PRD 10 `_check_numeric_parity`, before return) | — |
| 14-merge-provenance | T5 append `"merge_candidate_signal"` to `LOG_EXTRA_KEYS` | done | 070d071 | backend/utils/constants.py (one-line diff) | — |
| 14-merge-provenance | T6 signal tests | done | ec97aa9 | backend/tests/care_plan/test_pipeline_assembly.py | 3 signal tests |
| 14-merge-provenance | T7 test_constants assertion | done | 0d71064 | backend/tests/utils/test_constants.py | — |
| 14-merge-provenance | T8 full verification | done | no commit | — | 873 passed, ruff clean, no `merged: bool` anywhere in the codebase |

## Deviations from the PRD/TASKS
- T2 added `"source_fact_ids": [1]` to the three-way diagnosis-merge fixture, beyond what TASKS specified, because PRD 18's verification drops any diagnosis detail that lacks a citation — without it the fixture's diagnosis item would be silently filtered before the merge signal could be observed.
- TASKS T6's `-k merge_candidate_signal` pytest filter only selects 2 of the 3 tests actually added; the third test is named `...merge_candidate_count...` and doesn't match the filter string. All 3 pass; noted as a TASKS wording gap, not a code issue.

## Follow-up
PRD 18 has since landed (before 13/14, out of the README's recommended order — see 13's code run log), so `DiagnosisDetail` now carries `source_fact_ids`. `merge_candidate_signal` still only counts `_ITEM_LIST_FIELDS` (which includes `reason_for_visit` but not `diagnosis`/`diagnosis.details`), so diagnosis-detail merges are not counted by the aggregate even though the underlying field now exists. Extending the signal to diagnosis details would close the gap PRD 14 §9 already describes as a soft ordering dependency — but that extension isn't authorized by the current PRD or TASKS text, so it isn't done here. Routing to dev-design as a candidate small PRD 14 amendment.

## Still requires you (from PRD §8)
- Prompt smoke test against real notes, once this PRD's prompt text is live (ngrok + pm2, `SERVICE_MODE=combined`, against 2-3 real or realistic de-identified notes with a genuine multi-site finding — e.g. a cardiac catheterization or angiogram report naming plaque/stenosis/occlusion at three or more named vessels): confirm the model names all affected sites in the merged item rather than using a collective term or silently dropping one.
- Spot-check the `merge_candidate_signal` log line on the same smoke-test runs: confirm it appears in local/plain-text logs with sane `by_section` counts, and — if Cloud Logging access exists for a deployed environment by then — confirm the structured JSON payload actually carries the field once `K_SERVICE` is set.
- No new environment variables, credentials, or console configuration required — this sub-project is prompt text, one small pipeline.py diff, and tests only.
