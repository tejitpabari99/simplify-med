# Code Run — 2026-09-13 00:09 UTC — 01-schema-and-config (all tasks)
Branch: docs/fidelity-concision-brief   Base: main

## Tasks completed
| Sub-project | Task | Status | Commit | Files | Tests |
|---|---|---|---|---|---|
| 01-schema-and-config | Tasks 1+2 | done | 2a3ec4e | backend/models/care_plan/care_plan.py (combined, same file, per TASKS allowance) | — |
| 01-schema-and-config | Task 3 | done | 66e0522 | backend/utils/constants.py | — |
| 01-schema-and-config | Task 4 | done | ef24d47 | backend/models/ledger.py | — |
| 01-schema-and-config | Task 5 | done | 0143d7b | firestore.rules, backend/models/job.py | — |
| 01-schema-and-config | Task 6 | done | 40c38d7 | backend/app.py, backend/utils/constants.py | verified via subprocess env scenarios; no test file |
| 01-schema-and-config | Task 7 | done | 0206479 | backend/utils/misc.py | — |
| 01-schema-and-config | Task 8 | done | b247efe | backend/tests/fixtures/care_plan.json | — |
| 01-schema-and-config | Task 9 | done | 95bad55 | backend/tests/models/test_care_plan.py | 13 passed |
| 01-schema-and-config | Task 10 | done | b0484ce | backend/tests/models/test_ledger.py | 13 passed |
| 01-schema-and-config | Task 11 | done | 9462f33 | backend/tests/models/test_readpath_tolerance.py | — |
| 01-schema-and-config | Task 12 | done | c210b7b | backend/tests/models/test_job.py | 13 passed |
| 01-schema-and-config | Task 13 | done | cba649b | backend/tests/routes/test_jobs_e2e_scenarios.py | 38 passed; no size-budget threshold failures, so nothing to recalibrate for 06 from padding loss |
| 01-schema-and-config | Task 14 | done | 777f1b6 | backend/tests/utils/test_misc.py | 16 passed; added test_diagnosis_title_fallback |
| 01-schema-and-config | Task 15 | done with known failures | full suite | — | 6 failed, 545 passed, 24 subtests passed (`python3 -m pytest tests/ -q --no-cov`) |

## Verification detail (Task 15 / full suite re-run)

Command: `cd backend && python3 -m pytest tests/ -q --no-cov`
Result: **6 failed, 545 passed, 1 warning, 24 subtests passed** in 22.10s

Failing tests (all `pydantic_core.ValidationError: raw / Extra inputs are not permitted [type=extra_forbidden]`):
- `tests/care_plan/test_pipeline_schema.py::test_run_returns_care_plan_model_without_internal_scores`
- `tests/care_plan/test_pipeline_streaming.py::test_iter_steps_yields_step_events_in_order`
- `tests/care_plan/test_pipeline_streaming.py::test_iter_steps_yields_pipeline_run_result`
- `tests/care_plan/test_pipeline_streaming.py::test_iter_steps_wrap_step_called_per_step`
- `tests/care_plan/test_pipeline_streaming.py::test_iter_steps_step4_failure_falls_back_to_simplified`
- `tests/care_plan/test_pipeline_streaming.py::test_run_delegates_to_iter_steps`

Root cause (same in all 6): `backend/care_plan/pipeline.py:242-246` still builds the pipeline result with a `raw` key (`{"text": text, "simplified_text": simplified, "clarified_text": clarified}`), which is passed into `CarePlan.from_pipeline_result(result)` → `CarePlan.model_validate(data)` (`backend/models/care_plan/care_plan.py:125`, via `backend/models/base.py:24`). PRD 01's `CarePlan` schema (Tasks 1+2, commit `2a3ec4e`) removed the `raw` field and the model forbids extra fields, so validation fails with `extra_forbidden` on `raw` every time `care_plan/pipeline.py` runs the full pipeline.

Static checks:
- `grep -rn "Constants.Enums\|RawArtifacts\|additional_info\|main_conclusion" backend --include=*.py` → **zero hits** (confirmed, exit code 1 from grep = no matches).
- `ruff check .` (from `backend/`) → **All checks passed!**

## Deviations from the PRD/TASKS
- Task 15's criterion (zero failures except deferred size-budget ones) is **NOT fully met**: the 6 care_plan pipeline tests fail because PRD 01's schema removed `raw` while `care_plan/pipeline.py` (06's file, a PRD 01 non-goal) still passes `raw`/text keys into `CarePlan.from_pipeline_result` — i.e. the live pipeline is broken at runtime until 06 rewires it (or a minimal stop-gap removes those keys). See `backend/care_plan/pipeline.py:242-246` cited above.
- Task 14 added an extra `.title` fallback test (`test_diagnosis_title_fallback`), a small addition beyond the literal task text, justified by Task 7's acceptance criteria.
- Tasks 1+2 landed as one commit (`2a3ec4e`), per the TASKS.md allowance for combining same-file tasks.

## Follow-ups / new issues discovered
- The pipeline breakage above → PRD 06 (or a small stop-gap before 02–05 if a runnable pipeline is needed in the interim). Specifically, `care_plan/pipeline.py:242-246` needs its `result["raw"] = {...}` block removed or reworked to match the current `CarePlan` schema.
- Handed-off items to 06 from TASKS.md's "Handed off to PRD 06" section: strip `summary_fact_ids`/`source_fact_ids` in `routes/worker.py` (+ two tests); delete dead `.pop("raw", None)`; `_STRUCTURING_SCHEMA` exclude cleanup.
- Size-budget recalibration: not needed — Task 13's e2e scenario suite is all green (38 passed), so no padding-loss threshold adjustments are required for 06.
- `frontend/src/tests/fixtures/realCarePlanOutput.fixture.json` regeneration → deferred to 08.

## Still requires you
- Firebase Auth authorized domains for each ngrok domain (console).
- Set `CORS_ALLOWED_ORIGINS` locally per ngrok session.
