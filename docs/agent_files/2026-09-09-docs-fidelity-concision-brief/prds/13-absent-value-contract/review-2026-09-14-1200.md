# Review — 2026-09-14 12:00 — 13-absent-value-contract (covers 13 and 14 together)
Diff: e7e4d7f...0d71064   backend frontend   Personas: Bug Hunter, Security, Architect (run sequentially)

Note: this diff range spans both 13-absent-value-contract and 14-merge-provenance's commits (they landed back to back on the same branch base). This review covers both; see `14-merge-provenance/review-2026-09-14-1200.md` for a short pointer plus the 14-specific notes.

## Verdict: PASS

## Must-fix
- None.

## Notes (non-blocking)
- [Architect] `backend/tests/care_plan/test_pipeline_review.py:34` — commit 2b9a62b changed the sentinel fixture's `why` to `None`, silently overriding PRD 13 §4.4/§9's RESOLVED decision to leave that literal sentinel string untouched. Functionally harmless (the value is unasserted by that test). TASKS.md's Task 10 blanket grep acceptance criterion should be corrected to match the PRD rather than the other way around.
- [Bug Hunter] `backend/utils/term_detection.py` `render_care_plan_text` skips a `None` why (`add()` guards on falsy values), so the readability-score input no longer includes the old placeholder sentence. This is an intended behavior shift from the nullable-why change, not a bug.
- [Bug Hunter] Pre-existing, out of scope: the `_diff_item` dict branch uses `after_v[key]` without `.get()`, which could raise a `KeyError` instead of the expected `SimplifyError` if the corrector omits a key. Not introduced by this diff; flagged for awareness only.
- [Security] `None`↔`str` transitions on `why` are excluded from the lenient PII-substitution path by isinstance gating, so making `why` nullable tightens rather than weakens the corrector's guard. `escapeHtml`/React rendering of the null-fallback string is preserved.

## Next step
PASS → proceed.
