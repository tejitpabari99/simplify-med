# PRD 16 — Technical Documentation

Parent brief: `docs/agent_files/2026-09-09-docs-fidelity-concision-brief/brainstorm.v1.md` (approved; not re-litigated here). Owner instruction this PRD answers directly (not in the original brief): "When someone else accesses this repo, they should know the full flow, architecture, setup, infra — everything."
Branch: `docs/fidelity-concision-brief`.
Depends on: 01–09 (implemented, landed — the post-inversion pipeline this PRD documents), 10–15 (written in this batch, not yet implemented — §4.0 states the sequencing decision this PRD makes about them), 15 specifically for the RF/DJ/PD vocabulary and the `docs/uncalibrated-constants.md` register this PRD cross-references but does not own (§9).
Depended on by: 17 (scientific-documentation) — the two doc families cross-reference each other (§3); nothing else in this batch depends on 16 landing.

**This PRD is design-only.** It specifies what `docs/` should say and how it should be organized; it does not rewrite any doc itself (that is a `dev-tasks`/`dev-code` follow-on once this PRD is approved), and it does not touch `backend/`, `frontend/`, or any prompt file except where §4.6 proposes one new, narrowly-scoped test file.

## 1. Problem

The owner's own framing, verified against the actual repository state rather than assumed: **the entire `docs/` tree documents a pipeline that no longer exists.** This is not an exaggeration for effect — every one of the five files in `docs/` plus the root `README.md` contains at least one factually false sentence about the current system, verified below by reading the actual current code (`backend/care_plan/pipeline.py`, `backend/utils/constants.py`, `backend/models/job.py`, `backend/services/care_plan_input.py`, `backend/routes/jobs.py`, `backend/routes/worker.py`, `backend/models/provenance.py`) against every claim in every doc file, line by line.

### 1.1 The core factual error, repeated in five places

The real pipeline today is **four sequential LLM calls** — `ground` → `assemble_and_render` → `review` → `correct` (`backend/care_plan/pipeline.py:828-963`, `Constants.Pipeline.PIPELINE_STEPS` in `backend/utils/constants.py:62-76`) — plus a deterministic unitizer (`backend/services/unitizer.py`), deterministic term detection, three deterministic post-checks inside `assemble_and_render`'s `_verify_assembly` (citation existence, question-count cap, content-richness logging), a background glossary-curation thread running concurrently with steps 3–6, and a deterministic close (glossary re-detection). Six user-facing progress steps (`Constants.Pipeline.PIPELINE_STEPS`: `READ_NOTE`, `DETECT_TERMS`, `GROUND`, `ASSEMBLE_AND_RENDER`, `REVIEW`, `CORRECT`).

Every doc file instead describes the pre-inversion pipeline PRDs 04/05/06 deleted: five stages, three Gemini calls (`simplify_language` → `clarify_and_action` → `structure_note`), computed via three prompt files that **no longer exist on disk**:

```
$ ls backend/care_plan/prompts/
assemble_and_render.txt  correct.txt  curate_glossary.txt  ground.txt  review.txt  _style_rules.txt
```

`simplify_language.txt`, `clarify_and_action.txt`, `structure_note.txt` are gone. `docs/pipeline.md` still names all three as if they were live:

> `docs/pipeline.md:104`: "**Prompt** (`care_plan/prompts/simplify_language.txt`): rewrite the note to roughly a 6th-grade reading level..."
> `docs/pipeline.md:115`: "**Prompt** (`care_plan/prompts/clarify_and_action.txt`): rewrite in active voice..."
> `docs/pipeline.md:126`: "**Prompt** (`care_plan/prompts/structure_note.txt`): return JSON only..."

A reader who opens `pipeline.md` to understand what a document goes through, then goes looking for `simplify_language.txt` to see the actual prompt text, will not find it — because it was deleted by PRD 04, and the fields it produced (`simplified`, `clarified` on `PipelineRunResult`/`AdapterResult`) were deleted outright by PRD 06 §4.5 with nothing replacing them.

### 1.2 Why this is not cosmetic — the external-review harm

`docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md`, an external research review commissioned specifically to critique this product's clinical-fidelity design, used `docs/pipeline.md` as its **"repository baseline"** for what the pipeline does. Concretely: R2 (numeric integrity, now PRD 10), R1 (omission signal, now PRD 11), and R4 (extraction provenance, now PRD 12) all cite the old five-stage/three-call description as their premise — critiquing the product for defects **this branch had already fixed** at the time the review ran, because the docs still said the old pipeline was live. `_STYLE_RULES`'s numeracy gaps, the discarded `review().coverage` field, and the untagged OCR units are all real, still-open gaps this batch's PRDs 10–12 correctly identify — but the review reached them by reading a document that, for the pipeline shape itself, was already wrong. A stale `docs/pipeline.md` did not just fail to help; it actively fed a serious external analysis a false premise about what code was running. This is the single strongest argument in this repository for treating documentation rot as an engineering defect, not a tidiness backlog item — a wrong architecture doc is not neutral, it is actively misleading input to whoever reads it next, human or agent.

### 1.3 Full audit — every file in `docs/`, plus root `README.md` and `CONTRIBUTING.md`

Verified by direct comparison against the current code, PRD by PRD, at HEAD `43865b7`.

| File | Accurate | Stale (quoted) | Missing |
|---|---|---|---|
| `docs/README.md` | Index structure and links are fine. `architecture.md`'s and `data-and-privacy.md`'s descriptions are directionally correct. | `"the five simplification stages... the three sequential Gemini calls"` (line 7-8) — the pipeline is now four calls, six steps. | No mention of the unitizer, the ledger, or the deterministic close as topics `pipeline.md` covers. |
| `docs/pipeline.md` | §5 (term detection / jargon dictionaries) is fully accurate — `jargon_db.py`, the three JSON sources, alias expansion, longest-match-first ordering are all unchanged by any of PRDs 01–09. §8 (error taxonomy categories) is still accurate in substance. | §1: `"separated by a `--- Source: <filename> ---` marker"` (line 26) — `source_separator` was deleted outright by PRD 02 §4.6; file identity is now carried structurally via `SourceSpan`/`Unit`, never as inline text. §2: `"If the image's longest edge exceeds 2048 px"` (line 50) — PRD 02 §4.5 raised this to 4096px (`_MAX_LONG_EDGE_PX` in `utils/image_ocr.py`). §3: `"350,000 UTF-8-encoded bytes, checked alongside"` (lines 69-73) — PRD 09 deleted this second cap outright; `MAX_TEXT_LENGTH` (500,000 chars) is now the only length cap (verified: `backend/utils/constants.py`'s own comment on `MAX_TEXT_LENGTH` states this explicitly). §4 whole section (lines 79-142): the five-stage table, and every one of "Step 3 — simplify language", "Step 4 — clarify and action", "Step 5 — structure the document" — none of these methods, prompts, or fields exist any more. §6: `"computed from step 4's output (the clarified prose)"` (line 196) — `clarified` does not exist on any model; the after-score is computed from `render_care_plan_text(care_plan)` per PRD 06 §4.4/PRD 07. §7: `"raw text artifacts (original / simplified / clarified)... dropped"` (line 226) — `simplified`/`clarified` never exist in the first place now; only `raw` (also since deleted per PRD 01) was ever a field to drop. | The unitizer (`services/unitizer.py`), `SourceSpan`/`Unit`/`Fact` (the ledger), `ground()`, `assemble_and_render()` (including its three deterministic post-checks), `review()` (including the discarded-until-PRD-11 coverage signal), `correct()` (including the corrector-diff check), the glossary-curation background thread, and the deterministic close (glossary re-detection) — i.e. the entire current pipeline. |
| `docs/architecture.md` | §3 (API surface routes/response shapes), §4 (abuse protection/rate limiting), §6 (frontend structure) are all still accurate — none of PRDs 01–09 touch routes, rate limiting, or frontend structure beyond `ProcessingScreen.tsx`'s step array (already updated, verified against the live file: `frontend/src/components/ProcessingScreen.tsx:25-36` already has six `PIPELINE_STEPS` entries and `STEP_KEYS`). | §1 diagram: `"run the 5-step pipeline"` (line 36) — six steps now. §2 step 5: `"writing a stage update (1–5)"` (line 71) — range is now 1–6 (`complete_job`'s `"stage": Constants.Pipeline.PIPELINE_STEPS.CORRECT.number` = 6, per PRD 06 §4.7). §2 step 6: `"Both paths clear the job's raw `input_text` field"` (line 75) — **`JobDoc` has no `input_text` field at all any more** (verified: `backend/models/job.py`'s field list has no such key) — PRD 09 moved the raw text off Firestore into a GCS `JobInputPayload` object referenced by `input_payload_gcs_uri`; there is nothing left on the Firestore doc to "clear." "Job document as a state machine" §: `"input_text, input_pdf_gcs_uri"` (line 95) as key fields — `input_text` is gone; `input_payload_gcs_uri` is the real field, and it is a URI, not the text itself. §3 `POST /jobs`: `"a 500,000-character cap and, independently, a 350,000-UTF-8-byte cap (both must pass...)"` (line 122-123) — same PRD 09 staleness as `pipeline.md` §3. | The GCS `JobInputPayload` object (`gs://.../care_plan_inputs/{uid}/inputs/{uuid}.json`) as a first-class part of the topology — today's diagram shows only the merged-PDF GCS write, not the JSON payload write that is now the *only* channel carrying the note's text from API to worker. `services/unitizer.py::unitize` running once at the top of the worker's execution, before the pipeline call. |
| `docs/data-and-privacy.md` | Safety/scope statements, identity/auth section, rate-limiting section, third-parties section, and the general shape of "every deletion path" are all still correct in spirit. | `"Holds job status, stage, the resolved input text (until the job finishes)..."` (line 30) — the resolved input text never touches Firestore at all post-PRD-09; it lives only in the transient GCS payload object. Path 4, `"input_text field clearing"` (lines 41, 63-67) — this deletion path no longer exists in the form described (there is no Firestore field to clear); the equivalent protection today is that the GCS input-payload object is deleted unconditionally in the worker's `finally` block (`routes/worker.py:264-269`, alongside `input_pdf_gcs_uri`) and on `DELETE /jobs/<job_id>` (`routes/jobs.py:174-179`, alongside the merged-PDF object) — this is a genuinely different mechanism (object deletion, not field deletion) that needs its own path entry, not a patched-up description of the old one. | The GCS input-payload object's own deletion path is entirely undocumented — Path 3 ("Cloud Storage input cleanup") mentions only "the stored input PDF," but the worker's `finally` block deletes *two* GCS objects today (`input_pdf_gcs_uri` and `input_payload_gcs_uri`), and `DELETE /jobs/<job_id>` deletes both as well. This is a real, undocumented data-retention surface, not a nitpick — it is exactly the kind of claim `data-and-privacy.md` exists to get right. |
| `docs/deployment.md` | Fully accurate against `.github/workflows/deploy.yml`, spot-checked directly (resource names, `SERVICE_MODE` env vars, the `gemini-3.5-flash` model string, the bare-then-full two-pass deploy, the lifecycle-rule step, the retention Cloud Run Job). None of PRDs 01–15 touch infrastructure, deploy workflow, or GCP resource topology. | None found. | Nothing pipeline-related is missing here by design — this file's scope is infra, not pipeline shape, and infra hasn't moved. |
| `docs/local-development.md` | Fully accurate — env var tables match `backend/.env.example`/`utils/constants.py`'s `EnvVars`, test commands match `ci.yml` exactly, the `SERVICE_MODE=combined` explanation is unchanged by any of 01–09. | None found. | Nothing — this file's scope (running the app, not what the pipeline does) is untouched by the fidelity/concision rewrite. |
| Root `README.md` | Repository layout, ngrok section, quickstart pointer, license — all accurate. | `"runs the actual pipeline: deterministic term detection followed by three sequential calls to Gemini on Vertex AI"` (line 43) — four calls now. Documentation table row for `pipeline.md`: `"the five pipeline stages... the three Gemini calls"` (line 101) — mirrors `docs/README.md`'s same staleness. | Same gap as `docs/README.md` — no forward pointer to a flow document a newcomer could actually follow end-to-end. |
| `CONTRIBUTING.md` | Fully accurate — prerequisites, env var descriptions, test/lint commands, branch conventions, security-reporting process are all untouched by 01–09/10–15. No pipeline-shape claims appear in this file at all. | None found. | An explicit "update the docs" checklist item before merge (§4.6 proposes adding this). |
| `CHANGELOG.md` | The `[0.1.0]` entry is an accurate **historical** record of what shipped on 2026-09-07 — it correctly describes the pre-inversion, five-stage/three-call pipeline as it existed *at that time*. This is not stale prose; it is dated fact. | N/A — nothing in the existing entry is factually wrong for its own date. | **No entry exists for the fidelity/concision rewrite at all.** Nine landed PRDs (01–09) replaced the entire pipeline, changed the input-transport mechanism twice (02, then 09), and changed the OCR ceiling — none of this is reflected in the changelog. This is an omission a `git log` reader would not guess from the file itself: `CHANGELOG.md` currently implies the shipped v0.1.0 pipeline is still what runs today. |

**Bottom line: of six `docs/` files plus root `README.md`, four (`pipeline.md`, `architecture.md`, `data-and-privacy.md`, `README.md`) contain materially false statements about the pipeline or the job-input transport; two (`deployment.md`, `local-development.md`) are accurate and need no rewrite; `CONTRIBUTING.md` is accurate but should gain a process safeguard (§4.6); `CHANGELOG.md` is accurate-but-silent and needs one new entry.** This is not "some docs are a little behind" — it is that the one file (`pipeline.md`) most likely to be read as ground truth for "what does this system actually do" is wrong about its central subject from §4 onward (64 of its 250 lines), and the two files most likely to be cited by an external reviewer (`pipeline.md`, `architecture.md`) are exactly the two carrying the worst rot.

## 2. Goals

- Produce the audit above as this PRD's most load-bearing artifact (done, §1.3) — exhaustive, quoted, file-by-file, not impressionistic.
- Specify a target `docs/` structure (§4.1) that a new engineer, a deployer, and a reviewer can all use, stating explicitly where each currently-missing topic (the unitizer, the four-call pipeline, the deterministic checks, the glossary thread, the GCS input-payload transport, the six-step progress model) will live.
- Specify one **flow document** (§4.2) — the owner's explicit priority — deep enough that a newcomer can trace one note from upload to rendered care plan without opening a single PRD, including a mermaid diagram.
- Decide the **sequencing question**: does the doc rewrite land documenting the post-01-09 (current, implemented) state, or wait for 10–15 (written, not yet implemented) to land first? (§4.0 — recommendation: current state now, explicit forward note for 10–15, not a wait.)
- Define the **boundary with PRD 17** precisely enough that a future author never has to guess which doc family a given sentence belongs in (§3), and propose the literal cross-reference convention between the two families.
- Propose a **concrete, proportionate accuracy mechanism** (§4.6) — something that will actually get maintained, matching the existing `test_dead_code_removed.py` precedent already in this codebase, not invented ceremony.
- Settle where PRD 15's uncalibrated-constants register lives relative to this PRD's doc set (§9 — it is a `docs/`-level file this PRD's target structure must list and link to, but this PRD does not author its content; PRD 15 already specifies that content in full).

## 3. Non-Goals

- **Not the evidence/why layer.** PRD 17 (scientific-documentation) owns explaining *why* the pipeline is designed the way it is — the research corpus (AgenticSum, Asgari et al., Croxford/PDSQI-9, Fact-Controlled Diagnosis, the AHRQ Toolkit), how well-evidenced each design choice is, and a "good summary criteria" conformance table. This PRD documents **mechanism**: what each component does, its data shapes, its call sequence, its failure modes — never "how well-evidenced is this." The precise line, and the citation convention between the two families, is in §3 below (yes, this Non-Goals section names its own boundary section — see the worked rule immediately following).

  **The operational rule**: if a sentence's truth value depends on a cited external source or a comparison against a published instrument (PEMAT, SAM, CDC CCI, the Drive research corpus), it belongs in 17, and this PRD's docs cross-reference it rather than restating it. If a sentence's truth value depends only on reading the code (what function is called, what field exists, what happens on failure), it belongs here. Concretely: "the pipeline runs `ground` before `assemble_and_render`, which is fatal on failure" is 16's; "grounding runs before generation because atomization-before-generation was shown to worsen hallucination rates" is 17's — same architectural fact, but the *why* clause is the tell. Where a technical doc under this PRD needs to gesture at a design rationale (e.g. "review is non-fatal by design — see `docs/<17's-doc>.md` for why"), it states the mechanism and links out; it never re-derives or restates the evidentiary argument itself.

  **The literal cross-reference convention** (binding on both PRDs): a technical doc (this PRD's family) links to a scientific doc (17's family) with the phrase pattern `— see [<doc>.md](<doc>.md) for why` immediately after stating the mechanism, never before it (mechanism first, evidence-pointer second, so a reader who only wants "what happens" never has to skip past a citation to find it). A scientific doc links back to a technical doc with `— see [<doc>.md](<doc>.md) for the exact mechanism` when it needs to ground an evidentiary claim in a specific function or check. Neither family duplicates the other's prose; a link plus one clause of context (what the reader will find there) is the whole convention. This PRD's own docs (§4.1) leave the exact target filenames for 17's family as `[OPEN]` (§9) since 17 has not yet named its own doc structure at the time of this writing.

- **Not a tasks file.** `dev-tasks` generates `TASKS.md` from this PRD once approved; this document specifies *what* the rewrite must contain, not the junior-dev-followable steps to produce it.
- **Not a rewrite of any existing doc.** This PRD's only write is itself. No source file, no existing `docs/*.md`, no `README.md`/`CONTRIBUTING.md`/`CHANGELOG.md` is modified here.
- **Not a redesign of the pipeline, API, or data model.** Every fact this PRD documents is taken as given from the already-landed code (01–09) or the already-written PRDs (10–15, per §4.0's sequencing note). No architecture changes.
- **Not the RF/DJ/PD labeling convention or the uncalibrated-constants register's content.** Both belong to PRD 15, which has already specified them in full (`docs/uncalibrated-constants.md`'s exact seven-column format and seed rows). This PRD's only obligation is to place that file correctly in the target doc structure and link to it from the pipeline doc wherever a documented behavior is gated by a listed constant (§9).
- **No CI enforcement beyond the one narrowly-scoped test in §4.6.** No prose linter, no doc-coverage tool, no pre-commit hook that blocks a PR for missing doc updates beyond the `CONTRIBUTING.md` checklist line (§4.6) — matching PRD 15 §7's own reasoning against inventing tooling nobody will maintain.

## 4. Architecture Decisions

### 4.0 Sequencing — document the post-01-09 state now, not the post-10-15 state

**The ordering problem, stated precisely.** PRDs 01–09 are implemented and landed (verified throughout §1.3 by reading the actual code). PRDs 10–15 are written, reviewed to the same house standard, but **not yet implemented** — no code in `backend/` reflects any of them (verified: `grep -rn "extraction_method\|NUMERACY\|_UNIT_WORD_MAX_LENGTH"` against `backend/` returns nothing; `Medication.why` is still `str = ""`, not `str | None`). Writing the doc rewrite against a state that includes 10–15 would mean documenting fields, log lines, and prompt rules that do not exist in the checked-out code — the exact defect this whole PRD exists to eliminate, just moved forward in time instead of backward.

**Decision: the doc rewrite documents the post-01-09 (current, actually-running) state.** It does not wait for 10–15, and it does not speculatively describe what 10–15 will add. Reasons:

1. **A doc that describes code that doesn't exist yet is exactly as wrong as a doc that describes code that no longer exists** — both fail the one test that matters ("does this match what I'd find by reading `backend/`"). The owner's stated goal is that a newcomer can trust `docs/` as ground truth for the checked-out code; that promise is broken either direction.
2. **10–15 are five small, independent, mostly leaf PRDs** (per their own dependency headers: 10, 11, 12 are leaves; 13/14 share two files but are otherwise independent; 15 touches no code at all) — none of them changes the four-call shape, the six progress steps, the GCS transport, or the deterministic-close ordering this PRD's flow document (§4.2) is built around. Landing the doc rewrite now costs nothing structural when 10–15 later land; each one is a small, targeted **amendment** to an already-correct document (a new paragraph in the pipeline doc's `_verify_assembly` section for PRD 10's numeracy check, one new `Literal` value and a sentence in the data-model doc for PRD 12's `extraction_method`, a one-line nullability note for PRD 13's `why: str | None`), not a rewrite.
3. **Deferring the doc rewrite until 10–15 land compounds the exact problem this PRD is fixing**: `docs/` would stay wrong (in the *other* direction — silent about the current, real four-call pipeline) for the entire duration of five more implementation passes, each of which is itself a multi-day-to-multi-week `dev-code` effort. The current gap it fixes (stale by three PRDs' worth of change) is already causing external harm (§1.2); waiting only grows that gap before shrinking it.

**What this PRD's target docs must do about 10–15 concretely**: nothing speculative, but §4.1's per-file spec below flags, inline, exactly which section of which target doc each of 10–15 will need to touch when it lands (a one-line "PRD 10 adds a NUMERACY paragraph here" comment is fine in the *PRD*, not in the shipped doc — the shipped doc says only what is true today). This is recorded `[RESOLVED]` in §9.

### 4.1 Target `docs/` structure

| File | Disposition | Owns |
|---|---|---|
| `docs/README.md` | **Amended** (not rewritten — structure is fine, content pointers are wrong) | Index only. Update the `pipeline.md` and `architecture.md` one-line descriptions to match their real content (§4.1.2/§4.1.3) and add a pointer to the new flow document (§4.2) as the recommended entry point for a first-time reader. |
| `docs/flow.md` | **New** | The full request/job lifecycle end to end, with the mermaid diagram — this is the owner's stated priority document (§4.2). |
| `docs/pipeline.md` | **Rewritten** | The processing pipeline in depth: input extraction (unitizer, `SourceSpan`, page/line granularity per source type), the four LLM calls and every deterministic check around them, the glossary-curation thread, readability scoring, the six progress steps, the error taxonomy. §4.1.2 details exact contents. |
| `docs/architecture.md` | **Rewritten** | System topology (the two Cloud Run services + `SERVICE_MODE=combined`), the job document as a state machine (current fields only — no `input_text`), the API surface, Firestore/Cloud Tasks/GCS roles (including the `JobInputPayload` object, currently entirely undocumented), abuse protection, frontend structure. §4.1.3 details exact contents. |
| `docs/data-and-privacy.md` | **Rewritten** (data table and deletion-path table only; safety/identity/analytics sections carry over near-verbatim) | Every deletion path, corrected for the GCS input-payload object's own lifecycle (§1.3's finding). |
| `docs/deployment.md` | **Unchanged** | Already accurate (§1.3) — no PRD in this batch touches infra. |
| `docs/local-development.md` | **Unchanged** | Already accurate (§1.3). |
| `docs/uncalibrated-constants.md` | **New, but authored by PRD 15's `dev-tasks` follow-on, not this one** | PRD 15 §4.3's register, verbatim. This PRD lists it in the index and links to it from `pipeline.md`/`architecture.md` wherever a documented behavior is gated by a listed constant (§9). |
| `docs/error-taxonomy.md` | **New — split out of `pipeline.md` §8** | See §4.1.4 for why this earns its own file now. |
| `docs/testing.md` | **New** | How to run every test suite, what each test layer actually covers (unit vs. integration vs. prompt-content regression), and the doc-accuracy check from §4.6. Currently this content is split between `local-development.md` (commands) and nowhere (coverage philosophy) — `local-development.md` keeps the commands (unchanged, §4.1 table above), this new file explains what the suites are *for*, which is currently undocumented anywhere. |
| `README.md` (root) | **Amended** | Fix the two stale pipeline-shape sentences (§1.3); update the documentation table to add `flow.md`, `error-taxonomy.md`, `testing.md`, `uncalibrated-constants.md`. |
| `CONTRIBUTING.md` | **Amended** | Add the one-line doc-currency checklist item (§4.6). |
| `CHANGELOG.md` | **Amended** | One new entry for the 01–09 pipeline inversion (and, later, a second for 10–15 when they land) — dated at whatever date the doc rewrite actually merges, not backdated. |

**Why a new `error-taxonomy.md` and `testing.md` rather than folding everything into `pipeline.md`/`architecture.md`.** §1.3's audit found `pipeline.md` already carrying two topics that are not really "the pipeline" — the error taxonomy (§8, 20 lines, but growing: PRDs 10–15 add no new `ErrorCode` members per their own footprint discipline, but the *conditions* under which existing codes fire keep changing) and nothing at all about testing philosophy. The owner's stated audiences (a new engineer, a deployer, a reviewer) map unevenly onto the current five-file split: a reviewer auditing "what happens when X fails" today has to read all of `pipeline.md` to find §8 at the bottom; splitting it into its own file makes it independently linkable from `architecture.md`'s API-surface section (which already needs to reference it for the error-envelope shape) without a same-file forward reference. This is a **scope realignment, not new content invention** — every sentence in `error-taxonomy.md` and `testing.md` already has a home somewhere in the current docs or the PRDs; this just gives each topic its own address.

### 4.1.2 `docs/pipeline.md` — exact target contents

Rewritten top to bottom. Structure:

1. **Input extraction and unitization.** Per-format extraction (`extract_pages_from_bytes`, unchanged table from the current doc's §1 for TXT/PDF/DOCX/HTML/image dispatch — this part was and remains accurate), then the part that's new: `SourceSpan` and the unitizer. Explain `resolve_uploaded_files` building `provenance: list[SourceSpan]` at (file, page) granularity in the API process, the GCS `JobInputPayload` round-trip (§4.1.3 owns the transport mechanics; this section owns *why* unitization needs page/line identity — so grounding can cite `[unit_id]` instead of asking the model to invent a location), and `services/unitizer.py::unitize`/`provenance_for_pasted_text` materializing `list[Unit]` once, in the worker, immediately before grounding. Table of `Unit.file`/`Unit.page`/`Unit.line` semantics per source type (PDF real page numbers with gaps for blank pages; TXT/DOCX/HTML/image constant page 1; pasted text as the `"text_input"` sentinel) — this table already exists in PRD 02 §4.2 and is copied here verbatim, since it is pure mechanism with no PRD-internal reasoning attached.
2. **Image OCR** — current §2 content, corrected: 4096px downscale ceiling (not 2048), same Gemini-vision-call mechanism, same `NO_TEXT_FOUND` sentinel handling.
3. **Length limits** — corrected to the single `MAX_TEXT_LENGTH` (500,000 chars) cap only; state plainly that the former dual-cap design is gone and why (Firestore's 1 MiB limit no longer applies once input text lives in GCS, not Firestore — one sentence, cross-referencing `architecture.md`'s transport section rather than re-deriving PRD 09's argument).
4. **The pipeline: four LLM calls plus their deterministic checks.** This replaces the old "five pipeline stages" section entirely. One subsection per call:
   - **Term detection** (deterministic, unchanged) — current §4 "Step 2" content, verbatim, since PRDs 01–09 never touched `term_detection.py::detect_terms`.
   - **Grounding (`ground`)** — inputs (`units`, abbreviations), the `_GroundedFactRaw` → verified `Fact` pipeline, the three deterministic post-checks inside `_verify_ledger` (unit-existence, verbatim-quote check tolerant of OCR noise, the informativeness floor `_QUOTE_MIN_LENGTH`/`_QUOTE_LONG_WORD_MIN_LENGTH`), fatal-on-failure with no fallback, and why (no whole-document prose survives to fall back to — one sentence, not a re-argument of PRD 03's case).
   - **Assembly and render (`assemble_and_render`)** — the fact-ledger-to-typed-`CarePlan` mapping, the citation-existence check inside `_verify_assembly` (the property that replaced the deleted `close_coverage()`), the questions-cap-to-3 truncation, the content-richness logging pass, fatal-on-failure.
   - **Review (`review`)** — the fidelity-correction pass, `_sanitize_review_result`'s drop-and-log policy (unresolvable paths, `not_stated` outside the four `why` fields, contradiction guard where `remove` wins over `correct`/`not_stated` on the same item), non-fatal on failure (ships assembly's output unmodified).
   - **Correct (`correct`)** — applies named corrections plus a bounded PII sweep, the corrector-diff check (`_verify_correction_diff`) that rejects any unnamed, non-PII-shaped change, non-fatal on failure (reverts to the pre-correction `care_plan`).
   - **The glossary-curation background thread** — starts right after term detection, runs concurrently with all four LLM calls on its own `ThreadPoolExecutor(max_workers=1)`, the eager-`LLMClient()`-construction reasoning (avoids a `vertexai.init()` race with the main thread's own client), the 20-second timeout backstop and its fallback to uncurated terms.
   - **The deterministic close** — glossary re-detection against the final `care_plan` (`build_glossary_from_care_plan`), the only step left after `correct()` since the citation-existence property is already guaranteed true by construction from `assemble_and_render` onward.
   - A table: step number → label (patient-facing) → LLM call? → fatal/non-fatal → fallback — this is the direct replacement for the current doc's five-row table, now six rows, sourced directly from `Constants.Pipeline.PIPELINE_STEPS` and PRD 06 §4.9's composite policy table.
5. **Term detection and the jargon dictionaries** — current §5, unchanged (verified untouched by every PRD in 01–15).
6. **Readability scoring** — current §6, corrected: the "after" score is computed via `render_care_plan_text(care_plan)` (PRD 07), not "step 4's output." Everything else (the seven-dimension composite, the six named-method approximations) is unchanged code (`utils/scoring.py`) and stays as-is.
7. **Output shape** — current §7, corrected: no `simplified`/`clarified` artifacts ever exist to drop; only `raw` (deleted per PRD 01) and the internal fact-ID provenance fields (`summary_fact_ids`/`source_fact_ids`, stripped by `routes/worker.py::_strip_internal_provenance`) are pipeline-internal fields that never reach the frontend — say so explicitly, since this is a materially different (and more security/privacy-relevant) claim than "we drop some scratch text."
8. Cross-reference, not restate: the error taxonomy moves to `docs/error-taxonomy.md` (§4.1.4) — this section becomes one link plus one sentence.
9. **A footer note-block, in a clearly marked callout, pointing at `docs/uncalibrated-constants.md`** wherever a described behavior is gated by a listed constant — concretely, at minimum: the glossary-curation timeout (`GLOSSARY_CURATION_TIMEOUT_S`), the grounding quote-informativeness floor (`_QUOTE_MIN_LENGTH`/`_QUOTE_LONG_WORD_MIN_LENGTH`), and the OCR downscale ceiling (`_MAX_LONG_EDGE_PX`) — three of PRD 15 §4.3's five seeded rows are pipeline constants this very doc already needs to mention for mechanism reasons; the link adds "and here's how it was chosen, if you're wondering" without this doc re-deriving PRD 15's own reasoning.

### 4.1.3 `docs/architecture.md` — exact target contents

1. **Topology** — current §1, corrected diagram: six pipeline steps (or say "four LLM calls" rather than "5-step pipeline" — the diagram's job is topology, not pipeline depth, so keep it brief and correct rather than expanding it; full depth belongs in `pipeline.md`/`flow.md`).
2. **Request and job lifecycle** — rewritten against the real, current sequence:
   1. Submit (anonymous auth, `POST /jobs`) — unchanged.
   2. **Resolve input and write the GCS input payload** — this step's description changes completely: the API extracts text (unitizing spans, not concatenating with a separator marker), computes `SourceSpan` provenance, and writes **one JSON object** (`JobInputPayload{text, provenance}`) to GCS via `upload_job_input`, getting back `input_payload_gcs_uri`. The merged-PDF audit copy (`upload_combined_pdf` → `input_pdf_gcs_uri`) is a **separate, optional** GCS write for uploads only — the doc must distinguish these two GCS objects clearly, since today's doc only knows about one of them.
   3. Create the job — `JobDoc` now carries `input_payload_gcs_uri` and `input_pdf_gcs_uri` (both optional-shaped, though `input_payload_gcs_uri` is always populated in practice), never raw text or provenance inline.
   4. Enqueue — unchanged.
   5. Execute — the worker calls `load_job_input(job)` (one GCS read, downloading and parsing the `JobInputPayload`), then `unitize(text, provenance)` to materialize `list[Unit]` in memory, then runs the four-call pipeline, writing `stage` 1–6.
   6. Complete — the worker's `finally` block deletes **both** GCS objects (`input_pdf_gcs_uri` if present, `input_payload_gcs_uri` unconditionally) — corrected from today's single-object description.
   7. Display and delete — unchanged (cross-reference `data-and-privacy.md`).
   - **Job document as a state machine** — the field list is corrected to match `backend/models/job.py` exactly: `uid`, `status`, `stage` (1–6), `output_data`, `error_data`, `input_source_kind`, `input_source_filename`, `input_pdf_gcs_uri`, `input_payload_gcs_uri`, `input_version`, `grading_enabled`, `expires_at`, `skipped_files` — explicitly noting `input_text`/`input_provenance` **do not exist** on this model (a reader coming from the old doc, or from PRD 02 alone without reading PRD 09, would otherwise expect them).
   - **Idempotency and the processing lease** — unchanged mechanism, but the "double-billing every Vertex AI call" comment context updates to "four sequential LLM calls" (matching `routes/worker.py`'s own updated comment, PRD 06 §4.6).
3. **API surface** — current §3, corrected: `POST /jobs`'s validation description drops the byte-cap half of the old dual-cap description (§1.3's finding); everything else (multipart shape, response codes, `DELETE /jobs/<job_id>` now also deleting the payload GCS object per §1.3's finding) is corrected in place.
4. **Abuse protection** — unchanged, current §4 is accurate.
5. **Firestore, Cloud Tasks, and GCS** — current §5, with the GCS bullet split into two: the merged-PDF audit copy (existing description, unchanged) and **the new input-payload object** (new bullet: what it contains, its lifecycle — written once by the API, read once by the worker, deleted at job termination or explicit user delete, and its size profile — small integers plus one filename per (file, page), not a duplicate of the text, per PRD 02 §4.1's Option 2 accounting, cross-referenced not restated).
6. **Frontend structure** — current §6, unchanged (verified accurate against every file it names).

### 4.1.4 `docs/error-taxonomy.md` — exact target contents

Split out of `pipeline.md`'s current §8, expanded with the one thing the current doc never states explicitly: **which of the six pipeline steps can produce which class of error**, cross-referencing the fatal/non-fatal table `pipeline.md` now owns (§4.1.2). Structure: the `codes.py`/`exceptions.py` split (unchanged prose from today), the category list (unchanged), then a new table: step → possible `ErrorCode`s → fatal to the job? — sourced from PRD 06 §4.8's verified conclusion that 03/04/05 reuse `LLM_INVALID_JSON`/`PIPELINE_VALIDATION_FAILED` rather than adding per-step codes, and that the frontend's error rendering is already fully generic over `code`/`message`/`user_hint` (so this doc can state plainly: **no frontend change is ever required when a new failure mode is added to an existing step**, as long as it reuses an existing `ErrorCode` — a genuinely useful, currently-undocumented fact for a future contributor wondering whether touching error handling requires a frontend PR).

### 4.2 The flow document (`docs/flow.md`) — the owner's priority

**What it contains, and how deep it goes.** One continuous narrative, addressed to a reader who has never seen this codebase, tracing **one concrete note** from the moment a visitor drags a PDF onto the upload screen to the moment the rendered care plan appears and the job document is deleted. Depth target: deep enough that every function name, file, and Firestore/GCS write mentioned is a real, grep-able symbol (no hand-wavy "the backend processes the input" sentences) — but each step gets one paragraph plus one code/data pointer, not the full mechanism (that's `pipeline.md`'s and `architecture.md`'s job; `flow.md` sequences them, it does not re-derive them). Concretely, the thirteen beats:

1. Anonymous sign-in (`useAnonAuth.ts`) — one paragraph.
2. Upload/paste (`UploadScreen.tsx`, `validateFiles.ts`'s client-side mirror of the server caps) — one paragraph.
3. `POST /jobs` reaches `simplify-api` — `_resolve_job_input` extracts text, builds `SourceSpan` provenance, writes the GCS `JobInputPayload` — one paragraph, links to `architecture.md` §2 for the exact object shape.
4. `JobDoc` created in Firestore, `status="not_started"` — one paragraph, links to `architecture.md`'s job-doc field table.
5. Cloud Task enqueued, OIDC-signed, targeting `simplify-worker` — one paragraph.
6. `202 {"job_id": ...}` returns; frontend attaches the Firestore listener (`useJobSnapshot.ts`) and shows `ProcessingScreen.tsx` — one paragraph.
7. Worker picks up the task, verifies OIDC, checks the processing lease — one paragraph, links to `architecture.md`'s idempotency section.
8. `load_job_input` downloads and parses the GCS payload; `unitize()` materializes `list[Unit]` — one paragraph, links to `pipeline.md` §1.
9. Term detection runs; the glossary-curation thread starts in the background — one paragraph.
10. The four LLM calls run in sequence, each writing a `stage` update the frontend's listener picks up live — one paragraph per call is **too deep for this document**; one paragraph for all four, with the mermaid diagram (below) carrying the sequencing, and a link to `pipeline.md` §4 for depth.
11. The deterministic close (glossary re-detection); `output_data` assembled, internal fields stripped (`_strip_internal_provenance`) — one paragraph.
12. `complete_job` writes the terminal Firestore update; both GCS objects are deleted in the worker's `finally` block — one paragraph.
13. The frontend's listener sees the terminal state, renders `ResultScreen.tsx`/`CarePlanView.tsx`, fires `DELETE /jobs/<job_id>` the instant it mounts — one paragraph, links to `data-and-privacy.md`'s deletion-path table for the full backstop chain.

**The diagram.** One mermaid `sequenceDiagram` (renders natively on GitHub, no library needed), with four participants: `Browser`, `simplify-api`, `Firestore`, `simplify-worker` (folding GCS and Cloud Tasks in as implicit actions/notes on the arrows touching them, to keep the diagram to four swimlanes rather than six — a six-participant diagram at this level of detail becomes unreadable at normal render width, and GCS/Cloud Tasks are mechanism, not decision points, in this specific diagram's job). It must show, precisely:
- The `POST /jobs` call and its two possible GCS writes (payload always, merged PDF only for uploads) as a note on the `simplify-api` lifeline, not a separate participant.
- The Firestore job-doc creation and the Cloud Task enqueue as two distinct arrows from `simplify-api`, both before the `202` response returns to `Browser` — this ordering (config validated and both writes done before responding) is a real, load-bearing property (`architecture.md`'s "never orphans a doc" guarantee) and the diagram should make it visually obvious that the `202` comes *after* both.
- A loop/loop-like block on the `simplify-worker` lifeline representing the four-call pipeline, each iteration writing a `stage` update back to `Firestore` — collapsed to one labeled block ("ground → assemble_and_render → review → correct, writing stage 3-6"), not four separate LLM call arrows, to keep the diagram at flow-level altitude.
- A parallel/note annotation for the glossary-curation background thread, visually distinct (e.g. a note attached to the worker lifeline spanning the loop block) from the sequential four-call chain, since "this runs concurrently, not as a fifth sequential step" is exactly the kind of thing prose alone tends to blur and a diagram states unambiguously.
- The live Firestore listener as a dashed/async arrow from `Firestore` back to `Browser`, distinct from the synchronous `202` response — visually reinforcing that status updates are push-based, not polled (a fact `architecture.md` §1 already states in prose; the diagram should make a reader's mental model match it without re-reading the sentence).
- The two terminal GCS deletes (worker `finally` block) and the `DELETE /jobs/<job_id>` call (browser-initiated, on result-screen mount) as the diagram's final beats.

This is deliberately more detailed than a topology sketch and deliberately less detailed than a full call-graph — the flow document's whole reason to exist is to be the one artifact that answers "what happens to my note, in order" without requiring five other documents open in parallel tabs.

### 4.3 Data model and artifact lifetime — where does each doc's content actually live

Rather than a new file, this content is **distributed across the rewritten `architecture.md` (job doc + GCS objects, §4.1.3) and `pipeline.md` (unit/fact/care-plan shapes, §4.1.2)**, deliberately not concentrated into one "data model" document. Reasoning: every one of `Unit`, `Fact`, `SourceSpan`, `JobInputPayload`, `CarePlan`, and `JobDoc` already has a natural home in one of those two documents by virtue of *where it's used* (pipeline-internal vs. transport/persistence), and a newcomer asking "what does a `Unit` look like" is almost always already reading `pipeline.md`'s unitization section, not a separate schema reference they'd have to know to open. A single table **is** useful as a cross-cutting index, though — add one to `docs/README.md` (§4.1's "Amended" row): a flat table of every named type (`Unit`, `Fact`, `SourceSpan`, `JobInputPayload`, `CarePlan`, `JobDoc`, `ReviewResult`/`Correction`) with one line each — "what it is," "where it's defined," "where it lives and dies" (never persisted / GCS, transient / GCS, one-per-file / Firestore, one-per-file / Firestore-and-frontend) — and a link to the doc section that covers it in depth. This is the one piece of genuinely new cross-cutting content this PRD's structure adds beyond redistributing what already exists.

### 4.4 Setup, deployment, testing — confirmed no rewrite needed beyond `testing.md`

`local-development.md` and `deployment.md` need no changes (§1.3, §4.1 table). The new `docs/testing.md` (§4.1's table) is genuinely new content, not a correction — today, "how do I run the tests" lives in `local-development.md` and `CONTRIBUTING.md` (both correct, both just commands), but "what does each test layer actually verify, and where would I add a test for a new deterministic check" has no home anywhere. Contents: the directory layout (`backend/tests/{care_plan,models,routes,services,utils,integration,scripts}/`, `frontend/src/tests/`), what `test_pipeline_prompts.py`-style prompt-content regression tests check (that a named rule's literal text is present in a prompt file — PRD 04 §7.2's pattern, reused by PRD 10's planned `NUMERACY` block tests), what `test_dead_code_removed.py` checks (module-non-existence guards — the direct precedent this PRD's own accuracy mechanism, §4.6, extends), and a pointer to `docs/error-taxonomy.md`'s step→error-code table for anyone adding a new failure mode.

### 4.5 API and frontend change summaries

See §5/§6 below — both N/A, stated with reasons there rather than here, since the house format asks for them as top-level sections.

### 4.6 Accuracy mechanism — what actually gets maintained

**The precedent already in this codebase**: `backend/tests/integration/test_dead_code_removed.py` (§1, verified by reading) already asserts, mechanically, that specific deleted modules stay deleted (`utils.vertex_ai`, `utils.pdf_extract`, etc., each via `ModuleNotFoundError`) and that no stray `.py` file reappears at `backend/` root. This is the exact shape of check this PRD needs, aimed the other direction: not "assert the old thing is gone" but "assert the doc's claims about the current thing are still true."

**Proposed: one new test file, `backend/tests/integration/test_docs_reference_real_symbols.py`**, three narrowly-scoped checks:

1. **Every prompt filename `docs/pipeline.md` names in backticks must exist in `backend/care_plan/prompts/`.** Concretely: parse `docs/pipeline.md` for the regex `` `care_plan/prompts/([a-z_]+\.txt)` ``, and assert every match is a real file under `backend/care_plan/prompts/`. This is the exact test the task brief asked to consider, and it would have caught this PRD's entire §1.1 finding on day one of PRD 04 landing, had it existed then. Cheap, mechanical, zero false positives (a filename either exists or it doesn't), and it degrades gracefully — if a future author renames a prompt file and forgets the doc, this test fails with a clear message naming the missing file, not a vague doc-drift complaint.
2. **Every `Constants.Pipeline.PIPELINE_STEPS` member name `docs/pipeline.md`'s step table names must match the live enum**, by importing `Constants.Pipeline.PIPELINE_STEPS` and asserting its member count and each member's `.label` string appear verbatim in the doc's step table. This catches the single failure mode most responsible for the current rot (a pipeline restructure that changes step count/labels without a doc follow-up) directly, mechanically, at the exact seam PRD 06 already treats as load-bearing (its own `ProcessingScreen.tsx`/`markers.py`/`firebase.py` sync obligations, PRD 06 §4.1/§4.7).
3. **Every field name `docs/architecture.md`'s job-doc table names must be a real field on `JobDoc`**, via `JobDoc.model_fields.keys()` — catches the exact class of error this PRD's own audit found twice (`input_text` documented as existing when it doesn't).

**What this does NOT attempt, and why.** No check that prose *describing* a mechanism is accurate (e.g. "grounding is fatal on failure" — verifying this mechanically would require parsing English against `iter_steps`'s exception-handling structure, which is not a tractable, low-false-positive check) — that class of drift is caught by human review (the `CONTRIBUTING.md` checklist item below), not a test. This mirrors PRD 15 §7's own reasoning for its convention (no linter for prose, because a prose-correctness linter has a near-100% false-positive rate against ordinary technical writing) — the three checks above are chosen specifically because each is a **symbol-existence** check (a filename, an enum member, a model field), not a semantic one, which is exactly the class of drift that is both mechanically checkable and, per this PRD's own audit, the class that actually happened three times over in this repository's history.

**`CONTRIBUTING.md` checklist addition** (one line, under "Branch and PR conventions"): *"If your PR changes the pipeline's step sequence, call signature, prompt files, job-document schema, or GCS transport shape, update the corresponding section of `docs/` in the same PR."* This is a human-judgment backstop for the semantic drift the mechanical checks above cannot catch (e.g. a rewritten prompt whose *behavior* changes materially even though its filename doesn't) — realistic because it costs a reviewer one glance at the diff's file list, not a new workflow step, matching this repository's existing terse-checklist style (the file already has four bullet points of comparable weight under that heading).

**What is deliberately not proposed**: a CI job that fails a PR for "insufficient doc coverage" (unenforceable without a prose linter, per above), a doc-generation tool that produces `pipeline.md` from docstrings (this codebase's docstrings are PRD-history-dense and reviewer-voiced, not doc-reader-voiced — see PRD 06/02's own prose style throughout this repository's PRDs — auto-generating from them would produce a worse document than a deliberately-written one), or a scheduled/periodic "doc freshness" reminder (process ceremony nobody reliably acts on, per PRD 15 §7's identical judgment call for its own domain).

## 5. API Change Summary

N/A. This PRD changes no route, no request/response shape, no `ErrorCode`. It documents the API surface as it exists after PRDs 01–09; §4.1.3/§4.1.4 specify corrections to how that surface is *described*, not to the surface itself.

## 6. Frontend Change Summary

N/A for application code. **Doc-only**: none of `ProcessingScreen.tsx`, `ResultScreen.tsx`, `useJobSnapshot.ts`, or any other frontend file is touched — every frontend fact this PRD's target docs state (`ProcessingScreen.tsx`'s six-step array, `useJobSnapshot.ts`'s bare `stage` pass-through, `ResultScreen.tsx`'s generic error rendering) was independently verified against the live files during this PRD's own audit (§1.3) and found already correct; the frontend structure section of `architecture.md` needs no rewrite, only to survive unchanged into the new document (§4.1.3, item 6).

## 7. Testing

**How doc accuracy is verified**, honestly stated per the task's instruction not to invent ceremony:

1. **Mechanically, by the new test file** (§4.6): three symbol-existence checks (prompt filenames, pipeline-step labels, job-doc field names), run as part of the normal `pytest tests/ -q` suite (`ci.yml` already runs this on every push/PR — no new CI job needed, the new test file is picked up automatically by the existing `tests/` glob). This is real, automated, regression-proof coverage for the three specific classes of drift this PRD's own audit found actually happened.
2. **By human review, via the `CONTRIBUTING.md` checklist line** (§4.6), for the semantic drift the mechanical checks cannot reach (a prompt's behavior changing without its filename changing, a mechanism's failure-handling changing without its step count changing).
3. **By this PRD's own audit (§1.3) as the acceptance bar for the rewrite itself**: when `dev-code` implements this PRD's target structure, the concrete, checkable acceptance criterion is that every "Stale" cell in §1.3's table has a corresponding corrected sentence in the new doc, and every "Missing" cell has corresponding new content — a reviewer (or `dev-review`) can check the rewritten docs against this table cell-by-cell rather than re-auditing from scratch.

**What is not tested, and is not claimed to be**: whether the prose is *well-written*, whether the flow document's mermaid diagram renders correctly on GitHub (a one-time manual check at merge time, not a standing test — mermaid rendering is a GitHub platform feature, not this repo's code), and whether `docs/deployment.md`'s live resource names stay accurate as GCP infrastructure evolves (out of scope — no PRD in this batch touches infra, and a doc-vs-live-GCP-state check would require live cloud credentials in CI, a much larger and differently-scoped undertaking than this PRD's charter).

## 8. Manual Intervention Required From You

- **Confirm the sequencing decision (§4.0).** This PRD recommends documenting the post-01-09 state now rather than waiting for 10–15 to land. If you'd rather wait, say so and this PRD's §4.0 needs a different recommendation before `dev-tasks` runs.
- **Confirm the file-split decisions (§4.1)**: two new files (`docs/error-taxonomy.md`, `docs/testing.md`) plus the flow document (`docs/flow.md`), versus folding either back into `pipeline.md`/`architecture.md` if you'd prefer fewer files. All three are independently defensible either way; this PRD picked the split for the stated reasons but it's a judgment call, not a forced conclusion.
- **When 17 (scientific-documentation) is itself designed, confirm the cross-reference convention in §3 reads naturally from that PRD's side too** — this PRD wrote both halves of the convention since 17 doesn't exist yet as an approved PRD; whoever designs 17 should sanity-check that the link phrasing and boundary rule in §3 still make sense once 17's own doc filenames are chosen.
- **Approve (or redirect) before `dev-tasks` runs** — per this PRD's own charter, it is design-only; no doc gets rewritten and no test file gets written until this PRD is approved and handed to `dev-tasks`/`dev-code`.

## 9. Open Questions & Decisions

- `[RESOLVED: the doc rewrite documents the post-01-09 state now; PRDs 10-15's eventual landings are small, targeted amendments to an already-correct doc set, not a reason to wait — §4.0.]`
- `[RESOLVED: the boundary with PRD 17 is "mechanism here, evidence there," tested by whether a sentence's truth value depends on a cited external source — §3. The literal cross-reference phrasing ("— see X for why" / "— see X for the exact mechanism") is specified as binding on both PRD families.]`
- `[RESOLVED: docs/uncalibrated-constants.md is PRD 15's content (already fully specified there — location, seven-column format, seed rows) and this PRD's structure only lists it in the index and links to it from pipeline.md/architecture.md wherever a documented behavior depends on a listed constant. This PRD does not author, restate, or duplicate that register's content.]` This directly answers the task brief's coordination question: PRD 15 already made the ownership call (its own §9: "PRD 16 should link to it from `docs/pipeline.md`... PRD 17 should cite it") — this PRD adopts that call rather than re-deciding it.
- `[RESOLVED: the accuracy mechanism is three narrow symbol-existence tests (prompt filenames, pipeline-step labels, job-doc field names) plus one CONTRIBUTING.md checklist line for the semantic drift those tests cannot reach — no linter, no doc-generation tooling, no scheduled reminder — §4.6, matching PRD 15 §7's identical judgment against process ceremony for a comparable problem.]`
- `[OPEN] — exact target filenames for PRD 17's doc family, needed to finalize the literal cross-reference links this PRD's §3 convention specifies.` Cannot be resolved until 17 is itself designed; whoever authors 17 should either adopt this PRD's convention as-is or flag a conflict back here.
- `[OPEN] — whether `docs/testing.md` (§4.4) should also document the frontend's Vitest suite's coverage philosophy in the same depth as the backend's, or stay backend-focused with a shorter frontend pointer.` Both are defensible; this PRD leans backend-focused (more test *layers* to explain — unit/integration/prompt-content-regression/dead-code-guard — versus the frontend's simpler component-test-only shape) but doesn't force the call.
- `[OPEN] — whether `docs/flow.md`'s mermaid diagram should be duplicated (in reduced form) into root `README.md`'s "Architecture, in brief" section, replacing the current plain-text ASCII diagram there.` A mermaid diagram renders richer on GitHub than the current ASCII art, but root `README.md`'s diagram is deliberately terse (topology only, no lifecycle) — swapping it for even a reduced flow diagram might overweight the README relative to its own stated scope. Left to whoever implements this PRD to judge once `flow.md`'s actual diagram exists to compare against.
- `[DEFERRED] — retrofitting docs/deployment.md and docs/local-development.md with the same symbol-existence-test treatment (§4.6) once/if infra or env-var shape ever drifts.` Not needed today (§1.3 found both fully accurate), noted only so a future maintainer doesn't wonder why no test guards those two files.
