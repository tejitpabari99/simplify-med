# Tasks: Scientific Documentation

PRD: [`PRD.md`](./PRD.md) (this folder). Depends on: 15 (design-doc-evidence-labeling — this PRD **adopts** the RF/DJ/PD vocabulary PRD 15 §4.1 defines; nothing below redefines it, only cites and applies it) and 16 (technical-documentation — this PRD's five-file doc set slots into the `docs/` tree 16's task list creates/rewrites; see the ordering note below). Depended on by: none in this batch.

**Binding cross-PRD ownership (do not re-litigate):**
- **PRD 16 creates and owns `docs/uncalibrated-constants.md`** — its `TASKS.md` Task 1 authors the file (content itself specified by PRD 15 §4.3). **No task in this file may create or seed that file.** Tasks below only add markdown links *into* it from `evidence-map.md` and `design-rationale.md`.
- **PRD 15 defines the RF/DJ/PD vocabulary** (Research Finding / Design Judgment / Proposed Default) and its inline-tag format. This PRD's docs **use** that vocabulary — every task below that produces an RF/DJ/PD-tagged sentence cites PRD 15 §4.1 for the definition rather than re-explaining what RF/DJ/PD mean.

**Ordering dependency on PRD 16.** As of this task list's authoring, PRD 16's task list exists but has not executed: `docs/` today has only `README.md`, `pipeline.md`, `architecture.md`, `data-and-privacy.md`, `deployment.md`, `local-development.md` — no `docs/flow.md`, `docs/error-taxonomy.md`, `docs/testing.md`, or `docs/uncalibrated-constants.md` yet (those are PRD 16 Tasks 1/3/6/7). Every task below that links to one of those four not-yet-existing paths says so explicitly in its own text; the link text is still written now (per PRD §4's exact specified content), it just won't resolve until PRD 16's corresponding task lands. No task below is blocked from *landing* by this — a dead link to a soon-to-exist sibling file is normal in a coordinated two-PRD rollout and PRD §7 only requires link-checking "when each doc is authored... or again if substantially revised," not before PRD 16 merges.

**Conventions**

- One task per commit, in the order given. Branch: `docs/fidelity-concision-brief`.
- This is a documentation-only PRD: every task creates or edits one file under `docs/science/`. No `backend/`/`frontend/` code, schema, prompt, or test file is touched anywhere in this list (PRD §5, §6: N/A).
- No pytest/vitest suite applies here — acceptance criteria are `grep` assertions over the created markdown plus explicit human-readable structural checks, matching PRD §7's own posture ("no tooling is proposed... a reviewer confirms"). Where a task's acceptance criteria says "human-reviewed," that is inherent to the check, not a gap in this task list.
- Every task traces to a PRD `## 4. Architecture Decisions` subsection, cited inline. Do not add files, sections, or claims beyond what §4 specifies — in particular, do not vendor a third-party paper's text anywhere (§4.6, §3 Non-Goals: "not vendoring any third-party research paper" — binding on every task, not just Task 5).
- Carry the PRD's exact specified section headings and representative prose in §4.2-§4.6 verbatim into the corresponding file where the PRD gives them; do not paraphrase a heading or worked example the PRD already wrote out. Where the PRD gives a "representative sample" rather than the full content (§4.3, §4.4, §4.5), the task extends the same shape to full coverage (every pipeline-order decision, every criteria-doc row, every claim actually used elsewhere in the doc family) — this is called out per task, not an invitation to invent additional scope.
- No task in this file runs a backend/frontend test command — there is nothing to run.

---

### Task 1 — New `docs/science/README.md`

   - Files: `docs/science/README.md` (new file; also creates the `docs/science/` directory)
   - Dependency: none within this PRD (first file; other four link back to it, but it can be authored standalone since PRD §4.2 gives its full content). Its closing signpost links to `../pipeline.md` and `../architecture.md`, which exist today (pre-PRD-16) but will be rewritten by PRD 16 Tasks 2 and 4 — the links resolve either way since PRD 16 doesn't move or rename either file.
   - Changes (PRD §4.2): Create the file with the four-part outline PRD §4.2 specifies — (1) what the product does and does not try to do, (2) the "translate, don't interpret" boundary stated as a testable rule, (3) why fidelity and readability are in genuine tension, with the evidence, (4) a link table to the other four docs plus one closing pointer into 16's technical docs for mechanism. Carry PRD §4.2's "Representative content" block verbatim as the basis for this file — every heading (`# The scientific basis of simplify-med`, `## What this product is trying to do`, `## Fidelity and readability are in tension, and the evidence says so`, `## Reading this folder`), every sentence, the `(DJ)` and `(RF)` tags, the medication-reason forward-pointer to `good-summary-conformance.md`, the ophthalmology-report citation to `brainstorm.v1.md §2.5`, and the closing blockquote pointer to `../pipeline.md`/`../architecture.md`. This is the file's actual content, not a paraphrase target — reproduce it as given.
     Per §4.2's cross-reference rule: this file's closing blockquote pointer is the **one** orientation-signpost cross-reference into PRD 16's family used at the document's head/foot; do not add additional inline "see pipeline.md" links scattered through this file's prose beyond what the quoted block already contains.
   - Acceptance criteria:
     - The file exists at `docs/science/README.md` and is non-empty.
     - `grep -c "translate, don't interpret" docs/science/README.md` returns at least 1.
     - `grep -c "Not stated in your note" docs/science/README.md` returns at least 1 (the medication-reason forward-pointer sentence).
     - `grep -c "brainstorm.v1.md" docs/science/README.md` returns at least 1 (the ophthalmology-report citation).
     - `grep -c "design-rationale.md\]\|good-summary-conformance.md\]\|evidence-map.md\]\|research-corpus.md\]" docs/science/README.md` returns 4 (one link per sibling doc in the "Reading this folder" table).
     - `grep -c "pipeline.md\]\|architecture.md\]" docs/science/README.md` returns at least 2 (the closing pointer into PRD 16's docs).
     - `grep -c "(RF)\|(DJ)" docs/science/README.md` returns at least 2 (the two tagged sentences in the quoted content).

### Task 2 — New `docs/science/design-rationale.md`

   - Files: `docs/science/design-rationale.md` (new file)
   - Dependency: after Task 1 (this doc is linked from README.md's "Reading this folder" table, and its own header links back to `README.md`; also references `good-summary-conformance.md`'s numeracy section and `evidence-map.md` by name, both authored in Tasks 3-4 — those are forward references the PRD's own quoted content already contains, so no code changes here; land after Task 1 only to keep the family's cross-links internally consistent as they're authored). Links to `../uncalibrated-constants.md`, which does not exist in this repo yet — created by PRD 16 Task 1 (see this file's ordering-dependency note at the top). Write the link now per PRD §4.3's exact text; it resolves once PRD 16 Task 1 lands.
   - Changes (PRD §4.3): Create the file, one subsection per major pipeline-order decision (ground → assemble/render → review → correct → cross-cutting), each ending in an RF/DJ/PD-labeled claim per PRD 15 §4.1's convention (cited, not redefined). Carry PRD §4.3's "Representative content" block verbatim as the doc's actual content: the labels-recap paragraph naming PRD 15's convention and linking `../uncalibrated-constants.md`; and every one of the six worked sections shown — "Grounding precedes generation," "The ledger is clause-granular, not atomic," "Review never rewrites," "The corrector is diff-checked," "The forced-inference quotas were deleted," "Readability is telemetry, not a gate," and "Numeracy: no added label unless the source already has one" — including every citation (`arXiv:2310.08118`, `arXiv:2412.18004`), every RF/DJ tag, and every corroboration note against `comparison-drive-research-bundle.v1.md`. PRD §4.3 states this is "a sample of the full table this doc carries... the actual doc covers every row in this shape, not only the ones shown" — reproduce the seven shown sections in full (they are the PRD's actual specified content, not a subset to trim) and do not add invented additional sections beyond what §4.3 gives; if a future PRD adds a new pipeline decision, extending this doc is that PRD's own scope, not retroactively owed here.
     **Flag while authoring**: the "Numeracy" section's own text names "PRD 10's NUMERACY block" as the landed mechanism enforcing this rule. As of this task, `backend/care_plan/prompts/_style_rules.txt` has no NUMERACY block — PRD 10 is written, not yet implemented (confirmed by reading the live file: it contains only the PII paragraph and LANGUAGE RULES, no numeracy section). Carry the PRD's sentence as given (it is describing the intended/designed state, matching PRD 16 §4.0's own precedent of documenting the landed state while still citing forward-looking PRDs by number), but this task list flags it here so a reviewer doesn't mistake the doc's prose for proof the block has shipped — a future task (owned by whichever PRD lands PRD 10, per PRD §7's maintenance rule) updates this section once the block actually exists in `_style_rules.txt`.
   - Acceptance criteria:
     - The file exists at `docs/science/design-rationale.md` and is non-empty.
     - `grep -c "^## " docs/science/design-rationale.md` returns at least 7 (one per worked section: Grounding precedes generation; The ledger is clause-granular; Review never rewrites; The corrector is diff-checked; The forced-inference quotas were deleted; Readability is telemetry, not a gate; Numeracy).
     - `grep -c "arXiv:2310.08118\|arXiv:2412.18004" docs/science/design-rationale.md` returns at least 2 (each citation appears at least once).
     - `grep -c "(RF)\|(DJ)" docs/science/design-rationale.md` returns at least 7 (one per section's closing label, several sections carry more than one).
     - `grep -c "../uncalibrated-constants.md" docs/science/design-rationale.md` returns at least 1.
     - `grep -c "comparison-drive-research-bundle.v1.md" docs/science/design-rationale.md` returns at least 2 (the two corroboration notes — clause-granularity and forced-inference-quota sections).
     - `grep -c "PRD 15" docs/science/design-rationale.md` returns at least 1 (cites, does not redefine, the RF/DJ/PD convention).
     - `grep -c "NUMERACY" docs/science/design-rationale.md` returns at least 1 — human-reviewed to confirm the section reads as designed/specified (per PRD 10) rather than asserting it is already enforced in shipped code, given `_style_rules.txt` does not contain this block as of this task.

### Task 3 — New `docs/science/evidence-map.md`

   - Files: `docs/science/evidence-map.md` (new file)
   - Dependency: after Task 2 (its traceability table's "Used in" column cites `design-rationale.md`'s section names, which must already exist as written).
   - Changes (PRD §4.5): Create the file with two sections — (1) the evidence-to-design traceability table, (2) "What is not evidenced." Carry PRD §4.5's two "Representative content" tables verbatim: the six-row traceability table (arXiv:2310.08118 rubber-stamping finding, arXiv:2412.18004 post-hoc-citation finding, arXiv:2608.31016 enumerate-then-verify finding, Asgari et al. atomization finding, Croxford et al./PDSQI-9 finding, the readability-formula finding) with all five columns (Claim / Source / What it establishes / What it does NOT establish / Used in) exactly as given; and the eight-row "What is not evidenced" table (extraction recall, reviewer catch rate, questions-guard, line-ID attribution under OCR noise, the soundness-over-completeness inversion, every constant in `../uncalibrated-constants.md`, Asgari et al.'s transfer gap, PDSQI-9's transfer gap) with all three columns (Gap / Why it matters / What would close it) exactly as given, plus the section's opening paragraph ("This section exists so a future reader does not mistake 'reasoned' for 'measured'...").
     Link to `../uncalibrated-constants.md` (PRD 16 Task 1, not yet landed — see this file's top-level ordering note) rather than restating that file's seven columns.
   - Acceptance criteria:
     - The file exists at `docs/science/evidence-map.md` and is non-empty.
     - `grep -c "^## Evidence-to-design traceability\|^## What is not evidenced" docs/science/evidence-map.md` returns 2.
     - `grep -c "arXiv:2310.08118\|arXiv:2412.18004\|arXiv:2608.31016" docs/science/evidence-map.md` returns 3 (one per public-paper row).
     - `grep -c "Asgari\|Croxford" docs/science/evidence-map.md` returns at least 2.
     - `grep -c "research-corpus.md" docs/science/evidence-map.md` returns at least 1 (the Drive-sourced rows point readers to the vendored summary per §4.6, not to a bare Drive link).
     - `grep -c "^|" docs/science/evidence-map.md` returns at least 17 (2 headers + 2 separators + 6 traceability rows + 8 "not evidenced" rows, as a floor — allows for extra formatting rows).
     - `grep -c "../uncalibrated-constants.md" docs/science/evidence-map.md` returns at least 1.
     - `grep -c "mistake \"reasoned\" for \"measured\"\|reasoned.*measured" docs/science/evidence-map.md` returns at least 1 (the section's own framing sentence is present, not dropped).

### Task 4 — New `docs/science/good-summary-conformance.md`

   - Files: `docs/science/good-summary-conformance.md` (new file)
   - Dependency: after Task 5's file-existence is not required (this task authors the filled-in table; Task 5 vendors the *source* table into its own appendix per §4.6) — however, because §4.6's concrete form for `research-corpus.md` states the original criteria table is "reproduced verbatim in `good-summary-conformance.md`'s appendix," this task must land **before** Task 5 references it, and this task itself must include that verbatim-original appendix. Land this task after Task 2 (cites `design-rationale.md`'s numeracy section for the one convergence row).
   - Changes (PRD §4.4): Create the file reproducing the criteria doc's own category structure with three added columns (`Does Juno do it?`, `Mechanism or reason`, `How to verify`), carrying over the source doc's `Medical device?` column with every cell set to the explicit marker `Not assessed — requires regulatory/legal judgment (see §8)` (never a blank). Carry verbatim:
     - The "How to read the 'Does Juno do it?' column" block in full — all three verification kinds (prompt-text grep, schema/code fact, PRD citation) and the closing "No row in this table is marked Yes on the strength of this document's own prose alone" sentence.
     - The representative rows table exactly as given — all 11 rows shown (Simple/everyday words; Active voice; No acronyms/abbreviations; Numbers explained qualitatively; Percentages as frequencies; Evaluative labels/numeracy convergence; Remove distracting content; Explain medication reason; No adding/removing/exaggerating/diagnosing; Safety assessment; Personalization/health-literacy levels) — these are the PRD's explicit "range" sample including "the three tensions and the one convergence"; extend the same shape to every remaining row of the source criteria table (§4.4's outline: "reproduce the criteria doc's own category structure... the full doc carries every row"). Since the source criteria table's complete row list lives only behind the Google Doc this PRD's §4.6 declines to vendor a live link to, and the PRD's own text gives no further rows beyond these 11, this task's floor is: land all 11 PRD-specified rows verbatim, and add the closing `Medical device?` marker note once for the whole table (not per-row prose) exactly as the PRD states it. Do not fabricate additional criteria-doc rows beyond what PRD §4.4 actually quotes — a future revision that has access to the full source table extends this file; this task does not invent rows PRD 17 itself never saw.
     - Include the full original criteria-doc table (Category, Feature, Details, Priority, Medical device?, and the original empty "Does Juno do it?" column) as a verbatim appendix, per §4.6's "concrete form for `research-corpus.md`" instruction that this appendix — not `research-corpus.md` — is where the vendored-in-full original table lives (§4.6: "the complete original table... is reproduced verbatim in `good-summary-conformance.md`'s appendix, with the filled-in conformance columns added alongside, not in place of, the original"). Since this PRD's own quoted content does not give every original row's `Details` cell, use the same 11-row floor as the main table for the appendix, clearly labeled `## Appendix: original criteria doc (unmodified)`.
     - Two live-tension rows (remove-distracting-content vs. remove-nothing; medication reason vs. NOT STATED sentinel) are recorded per §4.4's outline as "**No — live design disagreement**," both positions stated, the shipped answer named, per this PRD's explicit Non-Goal (§3: "not resolving the three tensions... it does not adjudicate which one *should* ship").
   - Acceptance criteria:
     - The file exists at `docs/science/good-summary-conformance.md` and is non-empty.
     - `grep -c "^## How to read" docs/science/good-summary-conformance.md` returns 1.
     - `grep -c "prompt-text grep\|schema/code fact\|PRD citation" docs/science/good-summary-conformance.md` returns 3.
     - `grep -c "^|" docs/science/good-summary-conformance.md` returns at least 24 (main table: 1 header + 1 separator + 11 rows = 13; appendix table: 1 header + 1 separator + 11 rows = 13; floor of 24, allows extra).
     - `grep -c "live design disagreement" docs/science/good-summary-conformance.md` returns 2 (remove-distracting-content and medication-reason rows).
     - `grep -c "convergence" docs/science/good-summary-conformance.md` returns at least 1 (the numeracy convergence row).
     - `grep -c "Not assessed — requires regulatory/legal judgment (see §8)" docs/science/good-summary-conformance.md` returns at least 11 (once per main-table row — every row's `Medical device?` cell carries the marker, never blank).
     - `grep -c "^## Appendix: original criteria doc" docs/science/good-summary-conformance.md` returns 1.
     - `grep -c "design-rationale.md" docs/science/good-summary-conformance.md` returns at least 1 (the numeracy row's cross-reference to the convergence discussion).
     - `grep -c "backend/care_plan/prompts/_style_rules.txt" docs/science/good-summary-conformance.md` returns at least 3 (the "How to verify" column citing the real prompt file for the Language & Style rows) — spot-check the cited rule text against the live file: "Active voice. Address the patient as 'you.'" and "Expand every abbreviation" both appear verbatim in `backend/care_plan/prompts/_style_rules.txt` (confirmed during authoring — this task's rows describe landed behavior, not a forward-looking PRD).

### Task 5 — New `docs/science/research-corpus.md`

   - Files: `docs/science/research-corpus.md` (new file)
   - Dependency: after Task 4 (§4.6's own text points to `good-summary-conformance.md`'s appendix for the vendored-in-full criteria table rather than duplicating it here; this file's "Third-party research papers" table is also cited by `evidence-map.md`, Task 3, so land after Task 3 too — after Tasks 3 and 4).
   - Changes (PRD §4.6): Create the file per the vendoring decision — condensed summaries of the team's own eight Drive memos, a pointer to the criteria doc's full table (vendored in Task 4's appendix, not duplicated here), and a table of third-party papers cited-never-vendored. Carry PRD §4.6's "Concrete form" block verbatim: the opening paragraph, the `## Team memos (condensed, vendored)` section with its stated pattern (one subsection per memo, ~1 paragraph each, its own "repository observations vs. research findings vs. recommendations" framing, a link to the original Drive file) — the PRD's own text shows memos 00 and 03 as worked examples and elides 01/02/04/05/06/07 as "following the same pattern"; this task's floor is the two worked examples (00, 03) plus a placeholder subsection heading for each of 01, 02, 04, 05, 06, 07 (`### 01 — <topic>` etc.) marked `[condensed summary not yet authored — no Drive access from this task; see §8]`, since this task-authoring pass has no Drive access to condense memos this PRD's own text doesn't quote. Do not fabricate condensed content for memos 01/02/04/05/06/07 that the PRD text never gave — flag the gap explicitly rather than inventing summaries.
     - The `## The good-summary criteria doc (vendored in full)` section, verbatim, pointing to `good-summary-conformance.md`'s appendix (Task 4) rather than re-vendoring the table here.
     - The `## Third-party research papers — cited, never vendored` table, verbatim, all eight rows (AgenticSum, Asgari et al., Croxford et al./PDSQI-9, Fact-Controlled Diagnosis of Hallucinations, AHRQ Health Literacy Universal Precautions Toolkit, the three arXiv papers) with the "Where it's cited from" and "How to obtain it" columns exactly as given, plus the closing `[RESOLVED]` paragraph about the four Drive-only papers having no confirmed public identifier.
     - **Strict copyright boundary**: no task, including this one, copies any third-party paper's own text into the repo. The third-party table lists *where to find* each paper, never a paragraph of the paper's own content. This is §3's binding Non-Goal ("Not vendoring any third-party research paper") and §4.6's explicit reasoning ("This repo has no license to redistribute them").
   - Acceptance criteria:
     - The file exists at `docs/science/research-corpus.md` and is non-empty.
     - `grep -c "^## Team memos\|^## The good-summary criteria doc\|^## Third-party research papers" docs/science/research-corpus.md` returns 3.
     - `grep -c "^### 00\|^### 03" docs/science/research-corpus.md` returns 2 (the two PRD-given worked examples).
     - `grep -c "^### 01\|^### 02\|^### 04\|^### 05\|^### 06\|^### 07" docs/science/research-corpus.md` returns 6, each followed within a few lines by `grep -c "condensed summary not yet authored" docs/science/research-corpus.md` returning at least 6 (one per placeholder — no fabricated content for memos this task can't read).
     - `grep -c "AgenticSum\|Asgari\|Croxford\|Fact-Controlled Diagnosis\|AHRQ" docs/science/research-corpus.md` returns 5 (one per Drive-only/AHRQ third-party source).
     - `grep -c "arXiv:2412.18004\|arXiv:2310.08118\|arXiv:2608.31016" docs/science/research-corpus.md` returns 3.
     - `grep -c "\[RESOLVED\]" docs/science/research-corpus.md` returns at least 1 (the no-public-identifier paragraph).
     - `grep -c "good-summary-conformance.md" docs/science/research-corpus.md` returns at least 1 (points to the appendix rather than re-vendoring the table).
     - Human-reviewed: no paragraph in this file reproduces more than a short (PRD-quoted) fragment of AgenticSum/Asgari/Croxford/Fact-Controlled-Diagnosis/AHRQ's own text — the table only names where each paper lives and how to obtain it.

---

## Summary of what requires you (not a dev agent)

Per PRD §8, these are session-local judgment calls no dev agent can make:

1. **Decide the `Medical device?` column's actual classification.** Task 4's `good-summary-conformance.md` carries the explicit marker `Not assessed — requires regulatory/legal judgment (see §8)` on every row rather than a blank. Determining the actual FDA SaMD classification (or equivalent) is a regulatory/legal judgment outside this documentation's scope.
2. **Approve the vendoring decision (§4.6) before Task 5 and Task 4's appendix land** — specifically, confirm there is no licensing concern with condensing/summarizing (not reproducing verbatim, except the criteria doc's own table, which the team authored and has rights over) the eight Drive memos into this repo. This is a copyright/permission judgment call the PRD explicitly assigns to the owner, not to a docs pass.
3. **(Optional, unowned) A public arXiv/DOI identifier for the four Drive-only third-party papers** (AgenticSum, Asgari et al., Croxford et al., Fact-Controlled Diagnosis) would remove the single-point-of-failure risk Task 5's file records for those four specifically. Not required to ship this doc family; pick up only if someone has Drive access to the PDFs' own metadata or wants to run the literature search this PRD does not perform.
4. **Condense the six remaining Drive memos (01, 02, 04, 05, 06, 07) into `research-corpus.md`.** Task 5 leaves these as explicitly flagged placeholders because this task-authoring pass has no Drive access and this PRD's own text only worked through memos 00 and 03 in detail — someone with Drive access needs to read and condense the other six, following the same "repository observations vs. research findings vs. recommendations" framing.
5. **Cell-by-cell spot-check of `good-summary-conformance.md`'s falsifiability** (PRD §7's stated check) — a reviewer picking five rows at random and confirming the cited file/function/PRD section actually says what the row claims, rather than trusting this doc's own prose.
6. **One-time link check** (PRD §7) once PRD 16's `docs/flow.md`/`docs/error-taxonomy.md`/`docs/testing.md`/`docs/uncalibrated-constants.md` actually land — confirm every cross-reference this doc family makes into PRD 16's family resolves, since several are written now against files that don't exist in the repo yet (see the ordering-dependency note at the top of this file).

No `[OPEN]` items block this task list — PRD §9 was verified to contain none; every task above derives from a `[RESOLVED]` decision only.

No environment variables, credentials, or console access are needed anywhere in this task list (PRD §8: "prompt + pure Python only" doesn't even apply here — this is markdown only).
