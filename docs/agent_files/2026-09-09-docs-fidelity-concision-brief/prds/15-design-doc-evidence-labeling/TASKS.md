# Tasks: Design-Doc Evidence Labeling

PRD: [`PRD.md`](./PRD.md) (this folder). Depends on: nothing (PRD header: "Depends on: nothing. Touches no code, no schema, no prompt, no test."). Depended on by: 17 (scientific-documentation) adopts the RF/DJ/PD vocabulary this PRD defines; 16 (technical-documentation) may cite the constants register this PRD specifies. Neither is blocking.

**Reconciliation note on `docs/uncalibrated-constants.md` ownership (read this before doing anything below):** this PRD's own §4.3 and §8 say the file is "created by PRD 16 ... not a write this PRD makes itself, and not PRD 17's job either." I cross-checked both other PRDs' §9 directly:
- PRD 16 §9: `[RESOLVED: PRD 16 creates docs/uncalibrated-constants.md, seeded per PRD 15 §4.3's specification, and links to it from docs/pipeline.md; PRD 17 only links to it.]` — and PRD 16 §4.1's doc-inventory table lists it as "New — created by this PRD's `dev-tasks`/`dev-code` follow-on."
- PRD 17 §9: `[RESOLVED: PRD 16 creates and owns docs/uncalibrated-constants.md; PRD 17 only links to it.]`

All three PRDs agree, unanimously: **PRD 16's task list creates the file. This task list (PRD 15) does not**, even though PRD 15 §4.3 is where the file's content (seven columns, six seed rows) is actually specified. Nothing below creates `docs/uncalibrated-constants.md` — that file-creation task belongs in `prds/16-technical-documentation/TASKS.md`, not here. This task list only produces the one deliverable PRD 15 actually owns end-to-end: the comparison-doc retrofit (§4.4).

**Scope check — is there anything else for this task list to do?** Re-reading PRD §4.1 (label definitions) and §4.2 (where labels go): both are conventions *decided and recorded in the PRD text itself* — there is no separate artifact to write for either one; future PRD authors read PRD.md §4.1/§4.2 directly (or, per §4.5, would read a pointer in the skill template — see the out-of-repo item in the summary below). §4.3's register is PRD 16's file, not this one's. §4.4's retrofit decision is the one concrete, bounded, in-repo edit this PRD specifies but does not itself perform. §4.5 is entirely out-of-repo (see summary). That leaves exactly one deliverable for this task list: retrofit `comparison-drive-research-bundle.v1.md` with inline RF/DJ/PD tags, per §4.4's decision and §9's matching `[RESOLVED]` item.

**Conventions**

- One task per commit, in the order given. Branch: `docs/fidelity-concision-brief`.
- This is a documentation-only edit to one file (`docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md`). No backend/frontend code, schema, or prompt file is touched (PRD §5, §6: N/A).
- **No markdown lint or doc-lint tooling exists in this repo** — checked: no root `package.json`, no `.markdownlint*`/`.remarkrc*` file anywhere, and `frontend/package.json`'s `scripts` are `dev`/`build`/`tsc -b`/`vite build`/`lint` (ESLint, JS/TS only, not markdown)/`preview`/`test` (vitest). There is nothing to run beyond the grep assertions given per task and the human-review step PRD §7 already prescribes ("no CI check, no linter... the actual check is a human review step").
- Acceptance criteria below are grep assertions over the markdown file plus explicit human-readable structural checks — there is no pytest/vitest suite for this PRD (PRD §7: "There is no code to test").
- Every task traces to PRD §4.4 (the retrofit decision) and §4.1/§4.2 (the label definitions and inline-tag format being applied). Do not add scope the PRD doesn't decide: do not retrofit any of PRDs 01–09 (§4.4 explicitly decided against this), do not touch any section of the comparison doc other than the ones named per task, and do not invent a ledger table for the comparison doc (§4.2's exception clause would apply here since the file cites 3+ external sources, and the comparison doc already *has* its own de facto ledger — the `## Sources` section at the file's end already lists every source; do not duplicate it into a second table).
- **Section-boundary map** (line numbers as read at authoring time — locate by heading text if the file has since changed, not by line number): `## 1. Summary verdict` (13–37), `## 2. What the two approaches are` (38–68), `## 3. Where they agree` (69–121), `## 4. Area-by-area comparison` (122–382, subsections 4.1–4.8), `## 5. Recommendations — ranked` (383–505, R1–R7), `## 6. Explicitly rejected, with reasons` (506–520), `## 7. Where B is ahead of A` (521–563), `## 8. Open items this comparison surfaces` (564–579), `## Sources` (580–590).
- **Scope decision on "evidence-heavy sections"** — PRD §4.4 names "§5 recommendations table and its evidence-heavy sections" but does not enumerate the latter by number. I resolved this as follows, and both tasks below carry this resolution forward explicitly rather than leaving it to the executing agent's own judgment: **in scope** are §3 (Where they agree — the file's own header calls this "the main finding," built entirely from evidence-backed convergence claims), §4 (Area-by-area comparison, all of 4.1–4.8 — this is the section PRD 15 §4.4 itself cites by example: "its own §4.1 'reasoned default vs. guessed number' paragraph," referring to comparison-doc §4.1, not PRD 15's own §4.1), §5 (Recommendations, named explicitly), and §7 (Where B is ahead of A — a comparative evidentiary section of the same character as §3). **Out of scope, unchanged**: §1 (Summary verdict — a synopsis of §3–§8, not itself a place where new claims are made), §2 (What the two approaches are — descriptive/introductory; it lists B's cited papers but doesn't make an RF/DJ/PD-classifiable claim about them), §6 (Explicitly rejected — a rejection-rationale table, not an evidence-anchored claim table in the RF/DJ/PD sense), §8 (Open items — forward-looking, not claims to label), and `## Sources` (a citation list, not prose claims).

---

### Task 1 — Tag `## 5. Recommendations — ranked` (R1–R7) with inline RF/DJ/PD

   - Files: `docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md`
   - Changes (PRD §4.4, applying §4.1's definitions/tests and §4.2's inline-parenthetical-suffix format): Within `## 5. Recommendations — ranked` only (R1 through R7, currently lines 383–505), read every sentence and apply PRD §4.1's three tests. Append a bare parenthetical tag — ` (RF)`, ` (DJ)`, or ` (PD)` — at the end of each sentence that makes a substantive, classifiable claim, exactly as PRD §4.2's worked example shows (tag placed after the sentence's closing punctuation, on the same line). Do not reword, reorder, or delete any existing sentence — the only permitted change is appending a tag. Do not tag purely structural/descriptive sentences (e.g. "Cost S. ADOPT." classification lines, or a sentence that just names a file path with no claim attached).
     At minimum, apply tags to these identifiable claims (this list is a floor, not a ceiling — apply the same test to every other substantive sentence in R1–R7 too):
     - R1: the sentence citing Asgari et al.'s omission-vs-fabrication distinction ("The omission direction is exactly the failure mode A's evidence (Asgari et al.) identifies as genuinely distinct from fabrication...") — RF for the Asgari-attributed claim; the sentence connecting it to "made structurally invisible everywhere else in the pipeline" (this repo's own soundness-over-completeness choice) — DJ.
     - R2: the sentence contrasting `_style_rules.txt`'s current single numeric rule against A's Stage 3 numeracy section — DJ (a design gap identified by reconciling A's approach against this repo's actual prompt file).
     - R3: the sentence citing "documented in the brief's own cited literature as prone to rubber-stamping (arXiv:2310.08118)" — RF; the "Reduced form" recommendation paragraph (adopting a scaled-down version of A's mechanism for this product) — DJ.
     - R4: the sentence "This is, as the brief itself notes in its OCR-ceiling discussion, the one failure mode invisible to every other safeguard in the design" — DJ.
     - R6: the paragraph scoping down A's closed-vocabulary taxonomy to a single boolean flag ("unearned for a stateless product with no reviewer...") — DJ.
     - R7: this recommendation is itself PRD 15's own origin story — tag the sentence "B has several uncalibrated constants that are honestly flagged in prose but not systematically distinguishable from decisions backed by evidence: `_MAX_PII_TOKEN_DELTA=4`, `_QUOTE_MIN_LENGTH=12`, `GLOSSARY_CURATION_TIMEOUT_S=20`" with (PD) once per constant or once for the whole list (either is acceptable — PRD §4.2 doesn't mandate one tag per constant when several are listed together in one clause).
   - Acceptance criteria:
     - `grep -n "arXiv:" docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md` — every hit that falls within lines 383–505 (R3's `arXiv:2310.08118` reference) has `(RF)` appearing later in the same sentence or paragraph.
     - `grep -n "_MAX_PII_TOKEN_DELTA\|_QUOTE_MIN_LENGTH\|GLOSSARY_CURATION_TIMEOUT_S" docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md` — the R7 hit (within 383–505) has `(PD)` in the same sentence or paragraph.
     - `grep -c "(RF)\|(DJ)\|(PD)" docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md` returns at least 8 once this task lands (one tag per R1–R7 floor item above, R7 may use one tag for its constant list) — a human reviewer confirms each is placed on a genuinely classifiable sentence, not a filler insertion just to clear this count (PRD §7: "No acceptance criterion of 'X% of claims are labeled'... the convention succeeds if the next PRD... tags it").
     - `git diff -- docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md` shows only line-internal additions of ` (RF)`, ` (DJ)`, or ` (PD)` substrings — no line is otherwise reworded, no line is added or removed, no heading text changes.
     - Every other section of the file (`## 1` through `## 4`, `## 6` through `## Sources`) is byte-identical to before this task (confirmed by the same `git diff` showing zero hunks outside the 383–505 range).

### Task 2 — Tag `## 3. Where they agree`, `## 4. Area-by-area comparison` (4.1–4.8), and `## 7. Where B is ahead of A` with inline RF/DJ/PD

   - Files: `docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md`
   - Dependency: no PRD-decided ordering versus Task 1 (both edit the same file under this PRD's scope, with no cross-reference between them) — land after Task 1 only to avoid two agents editing the same file concurrently, not because the PRD requires this order.
   - Changes (PRD §4.4, same tests/format as Task 1): Within `## 3` (69–121), `## 4` (122–382, all of 4.1–4.8), and `## 7` (521–563) only, apply PRD §4.1's tests and append the same inline ` (RF)` / ` (DJ)` / ` (PD)` tags. Same rule as Task 1: append-only, no rewording, no tagging of purely structural bullet lead-ins.
     At minimum, apply tags to these identifiable claims:
     - §3's bulleted convergence list: each bullet that names a specific citable source for one side's decision (e.g. "A's Stage 1→Stage 3 ordering," "A calls this invariant 5," "brainstorm §3.2," `arxiv` references at lines 61–62 if within scope — note these two lines are in §2, out of scope per this task list's scope decision above, do not tag them here) should get `(DJ)` where the bullet's own claim is "two independently-reasoned designs converged" (a design-judgment-level comparative claim, not a directly-cited research finding) — apply per-bullet, not once for the whole list.
     - §4.1's closing paragraph ("A broader pattern worth naming once... B picks and documents concrete tunables everywhere... flags them honestly as not-yet-calibrated rather than blocking implementation... but it is also exactly the gap R7 targets") — tag `(DJ)`; this is the exact paragraph PRD 15 §4.4 itself points to as "a direct, un-tagged instance of the confusion R7 diagnoses" and is why this file was chosen as the one retrofit exception.
     - §4.1's mention of `_QUOTE_MIN_LENGTH=12`, `_QUOTE_LONG_WORD_MIN_LENGTH=7`, `_MAX_PII_TOKEN_DELTA=4` in that same paragraph — tag `(PD)` (may share the same sentence-level tag as the `(DJ)` above if both apply to the same sentence; PRD §4.2 allows a sentence to carry more than one tag when it does more than one kind of work — e.g. `[RESOLVED: ...] (PD)` already demonstrates a sentence carrying two tags of different axes, though same-axis double-tagging here means splitting the sentence at the clause boundary if a single tag can't honestly cover both the comparative judgment and the constant-listing).
     - §4.3 "Evidence anchoring and grounding checks" — tag any sentence citing Asgari et al. or an arXiv ID `(RF)`, and any sentence reconciling that evidence with this repo's specific check (`_is_verbatim_quote`, `_verify_ledger`) `(DJ)`.
     - §7's PII bullet ("A never mentions PII... B recognized this needs a deliberate, source-unfaithful exception and implemented it twice...") — tag `(DJ)` (a product judgment made in the absence of, not in reconciliation with, external evidence — still DJ per §4.1's definition, since DJ only requires an in-repo-constraint connective, not that evidence exists on the other side).
     - §7's clause-granularity bullet ("Clause granularity as the more internally consistent reading of the shared Asgari evidence") — tag `(RF)` for the Asgari attribution and `(DJ)` for "the more internally consistent reading" judgment, matching PRD 15 §4.1's own Worked Example 2 (this is literally the same clause-granularity decision PRD 15 uses as its DJ example).
   - Acceptance criteria:
     - `grep -n "Asgari\|arXiv:" docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md` — every hit within lines 69–382 or 521–563 has `(RF)` in the same sentence or the immediately following clause.
     - The §4.1 closing paragraph (containing "A broader pattern worth naming once") contains both `(DJ)` and `(PD)` (or a documented sentence-split doing the same job) after this task lands.
     - `grep -c "(RF)\|(DJ)\|(PD)" docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md` increases by at least 10 from this task's edits alone (on top of Task 1's count) — human-reviewed for placement quality, not just count (same PRD §7 posture as Task 1).
     - `git diff -- docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md` (this task's own diff, on top of Task 1's already-landed commit) shows hunks only within lines 69–382 and 521–563; `## 1`, `## 2`, `## 5` (already tagged by Task 1, untouched here), `## 6`, `## 8`, and `## Sources` are byte-identical to their post-Task-1 state.

### Task 3 — Verify scope boundaries and run the final grep sweep

   - Files: none (verification only)
   - Changes: none.
   - Dependency: land after Task 1 and Task 2.
   - Acceptance criteria:
     - `grep -n "(RF)\|(DJ)\|(PD)" docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md` — every matching line number falls within one of: 383–505 (§5, Task 1) or 69–382, 521–563 (§3/§4/§7, Task 2). Zero matches in 1–68 (§1–§2), 506–520 (§6), 564–590 (§8, Sources).
     - `git diff main -- docs/agent_files/2026-09-09-docs-fidelity-concision-brief/comparison-drive-research-bundle.v1.md` (or `git diff <branch-point>..HEAD` for the same file) touches only this one file in the repo — confirms neither Task 1 nor Task 2 drifted into editing `PRD.md`, `prds/README.md`, or any other file.
     - No `backend/`, `frontend/`, or prompt file appears anywhere in either task's diff (PRD §5/§6: N/A) — `git diff main --stat` for the commits covering Tasks 1–2 shows exactly one file changed.
     - Re-read the four floor-item lists in Task 1 and Task 2 against the final file: every named claim carries its specified tag. This is the one-time content-freshness check PRD §7 describes for a documentation convention — not a standing test, run once now.

---

## Handed off to other sub-projects (specified here, not implemented here — do not action as part of this task list)

- **To PRD 16 (technical-documentation)**: creation of `docs/uncalibrated-constants.md` itself, seeded verbatim from this PRD's §4.3 tables (seven columns, six seed rows: `_QUOTE_MIN_LENGTH`, `_QUOTE_LONG_WORD_MIN_LENGTH`, `_MAX_PII_TOKEN_DELTA`, `GLOSSARY_CURATION_TIMEOUT_S`, `_MAX_LONG_EDGE_PX`, `_UNIT_WORD_MAX_LENGTH`), plus linking it from `docs/pipeline.md`. See `prds/16-technical-documentation/TASKS.md` (not this file) for that task. Confirmed by reading PRD 16 §9 and PRD 17 §9 directly — both agree PRD 16 owns file creation; PRD 15 (this PRD) owns only the content specification, already complete in `PRD.md` §4.3, requiring no further task here.
- **To PRD 17 (scientific-documentation)**: adoption of the RF/DJ/PD vocabulary in its own `docs/science/*.md` prose, and linking to (never restating) `docs/uncalibrated-constants.md`. Not this PRD's task list.

## Summary of what requires you (not a dev agent)

Per PRD §8 and §4.5, these cannot be executed by a dev agent as part of this task list:

1. **Add the one-line pointer to `design-agent-prompt-template.md`'s HOUSE FORMAT block** (PRD §4.5), so a future cold `dev-design` run surfaces the RF/DJ/PD convention automatically. The PRD cites this file at `/root/.claude/skills/dev-design/design-agent-prompt-template.md`. I checked both paths named or implied anywhere in this PRD batch: **both `/root/.claude/skills/dev-design/` and `/root/.claude2/skills/dev-design/` exist on this machine and are byte-identical** (`design-agent-prompt-template.md`, 3591 bytes; `SKILL.md`, 5814 bytes — `diff` confirms no difference). Neither is inside this repo (`simplify-med`); both are machine-level skill configuration shared across every project this machine manages. The exact edit, per PRD §4.5, is a new line after the `## 4. Architecture Decisions` line in the HOUSE FORMAT block (currently at line 33 of `design-agent-prompt-template.md` in both copies): add `"When a claim is directly reported by a cited external source, or is a threshold/default picked without measurement, consider tagging it inline — see docs/agent_files/2026-09-09-docs-fidelity-concision-brief/prds/15-design-doc-evidence-labeling/PRD.md §4.1–§4.2 for the convention and worked examples."` Worded as "consider," not "must," per PRD §4.5/§7. Since this touches machine-wide configuration outside any project repo, it should go through the `update-config` skill or a direct edit by you — not through this repo's `dev-tasks`/`dev-code` flow, and not duplicated across both `.claude`/`.claude2` copies without your say-so on which (or both) should change.
2. **(Optional, per PRD §4.5) The same pointer in `dev-tasks`' own template**, if/when it ever generates prose that makes evidentiary claims — PRD 15 notes it currently doesn't (tasks files are implementation steps, not claims about evidence), so this is explicitly optional and not blocking.
3. No environment variables, credentials, deploys, or console access are needed anywhere in this PRD (§8: confirmed, nothing further here).

No PRD §9 items are `[OPEN]` — confirmed by direct reading: all nine bullet items in §9 are `[RESOLVED: ...]` except one `[DEFERRED]` (automated enforcement tooling, explicitly not wanted). Every task above derives from a `[RESOLVED]` decision only.
