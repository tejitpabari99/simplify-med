# Code Run — 2026-09-13 02:44 — PRD 03 (grounding), all 9 tasks
Branch: docs/fidelity-concision-brief   Base: f21b78b

## Tasks completed

| Task | Status | Commit | Files | Tests |
|---|---|---|---|---|
| Task 1 — `normalize_with_offsets`, `normalize_text` as wrapper | done | 00ab19d | backend/utils/text_normalization.py | — |
| Task 2 — `ground.txt` prompt with the eight-category checklist | done | c6c17e5 | backend/care_plan/prompts/ground.txt | — |
| Task 3 — grounding prompt load, `_GroundedFactRaw`, `_GROUNDING_SCHEMA` | done | 8cc3b92 | backend/care_plan/pipeline.py | — |
| Task 4 — deterministic ledger checks and offset recovery | done | ab03cf8 | backend/care_plan/pipeline.py | — |
| Task 5 — `CarePlanPipeline.ground()` | done | ea0f191 | backend/care_plan/pipeline.py | — |
| Task 6 — ground prompt tests | done | 63b30de | backend/tests/care_plan/test_pipeline_prompts.py | 5 tests added (20 total in file) |
| Task 7 — ground schema and LLM-boundary tests | done | 053452d | backend/tests/care_plan/test_pipeline_schema.py | 5 tests added (13 total in file) |
| Task 8 — grounding verification and offset-recovery tests | done | f7b6cc6 | backend/tests/care_plan/test_pipeline_grounding.py (new file) | 26 tests added |
| Task 9 — full-suite verification | done | (no commit — verification-only) | — | see below |

## Verification detail (Task 9)

All four commands run from `backend/` with `/tmp/vv` activated (repo root for the `grep`), per the environment note.

1. **Full suite** — `python -m pytest tests/ -q`:
   ```
   608 passed, 1 warning, 24 subtests passed in 98.74s (0:01:38)
   ```
   Zero failures, zero errors. The 1 warning is a pre-existing `DeprecationWarning` from `PyPDF2/__init__.py:21` ("PyPDF2 is deprecated. Please move to the pypdf library instead.") — unrelated to this PRD's changes, not attributable to grounding work.

2. **Wiring smoke import**:
   ```
   python -c "from care_plan.pipeline import CarePlanPipeline, _GROUND_PROMPT, _GROUNDING_SCHEMA; from utils.text_normalization import normalize_text, normalize_with_offsets; assert hasattr(CarePlanPipeline, 'ground')"
   ```
   Exited 0, no output — succeeded.

3. **No-wiring boundary grep** (from repo root):
   ```
   grep -n "self.ground(\|\.ground(units" backend/care_plan/pipeline.py
   ```
   No matches (exit code 1). Confirms `ground()` has no call site inside `iter_steps` or anywhere else in `pipeline.py` — Task 5's "no wiring" boundary held through to the end of this PRD's work.

4. **Lint** — `ruff check .` (from `backend/`):
   ```
   All checks passed!
   ```

5. **Diff-stat scope check** — `git diff --stat f21b78b..HEAD`:
   ```
    backend/care_plan/pipeline.py                      | 251 +++++++++++++
    backend/care_plan/prompts/ground.txt               |  39 ++
    backend/tests/care_plan/test_pipeline_grounding.py | 395 +++++++++++++++++++++
    backend/tests/care_plan/test_pipeline_prompts.py   |  41 +++
    backend/tests/care_plan/test_pipeline_schema.py    |  91 +++++
    backend/utils/text_normalization.py                |  85 ++++-
    6 files changed, 897 insertions(+), 5 deletions(-)
   ```
   Exactly the six files PRD 03 was expected to touch — no changes outside this PRD's scope.

All four of Task 9's acceptance criteria pass. No production or test file was changed during this verification.

## Deviations from the PRD/TASKS

1. **Task 3 temporarily left four `F401` unused-import findings** (`Fact`, `Unit`, `normalize_text`, `normalize_with_offsets`) after adding the imports ahead of the code that uses them; **Task 4 resolved all four** by using them in the deterministic checks and offset-recovery code it added. This split is deliberate and matches the PRD's own task boundary (Task 3 = schema/prompt-load scaffolding, Task 4 = the logic that consumes it) — `ruff check .` is clean at the end of Task 4 and remains clean through Task 9's re-check above.
2. **Task 8 added one extra, unrequested test**, `test_locate_quote_offsets_recovers_full_span_across_tab_noise`, alongside the double-space whitespace-noise variant. The PRD's whitespace-noise case names "double space or tab" as two illustrative examples of the same normalization behavior; this test exercises the second example in addition to the first. It replaces or renames nothing already specified.
3. **Task 8's informativeness-boundary test builds its fixture programmatically** from `_QUOTE_MIN_LENGTH` / `_QUOTE_LONG_WORD_MIN_LENGTH` (imported directly from `care_plan.pipeline`) rather than hardcoding the literals 12 and 7. Confirmed in `tests/care_plan/test_pipeline_grounding.py`: both constants are imported at the top of the file and used to construct `_short_word_phrase(...)` fixtures at and just below each threshold, rather than embedding the numbers. This keeps the tests correct if either constant is retuned later, per the PRD §9 resolution noting both constants are "tunable independently."

## Follow-ups / new issues discovered

None. The full suite is green, lint is clean, the diff footprint matches the PRD's expected file list exactly, and the no-wiring boundary holds.

## Still requires you (from PRD §8 + this run)

**Prompt smoke test against real notes.** PRD §8 calls out that no automated test in this sub-project (or anywhere in the repo, per the brief's Non-Goals) checks clinical fidelity — that requires a real Vertex AI call and a judgment call on the output, so it is listed here rather than in §7. It can only run **once PRD 06 wires `ground()` into the live pipeline** (`iter_steps`, `Constants.Pipeline.PIPELINE_STEPS`), since no call site exists yet (confirmed by this run's grep in item 3 above). Once wired, run it via ngrok + pm2 (`SERVICE_MODE=combined`) against 2-3 real or realistic de-identified notes, specifically checking:

- (a) **Contrast-dye classification** — a contrast-dye-during-imaging sentence lands in `tests`/`procedures`, not `medications`. This is the direct fix for the misclassification bug the eight-category checklist exists to address (brief §1).
- (b) **Abbreviation expansion in `text` while `quote` stays verbatim** — a dense abbreviation line (e.g. `f/u cards 4/12`) should produce a `Fact` whose `text` field carries the expansion, while the underlying quote used for verification/offset-recovery remains a verbatim substring of the source unit (no expansion applied to the quote itself).
- (c) **65,536-token output budget on a realistic multi-page note** — confirm a realistic multi-page note does not hit `LLM_MAX_TOKENS` at the `MAX_TOKENS_LONG_FORM` (65,536) ceiling; per PRD §9's resolution, no chunking/batching is implemented, so an overflow here would be a fatal grounding failure under §4.6 and would need revisiting.

No new environment variables, credentials, or console configuration are needed for this — prompt + pure Python only, per PRD §8.
