# Code Run — 2026-09-13 05:45 UTC — 06-pipeline-orchestration

Branch: `docs/fidelity-concision-brief`
Base: `d130b9283f3b0d0d6fb440f1e38e10cf1688f9bf`

## Tasks completed

| Sub-project | Task | Status | Commit | Files | Tests |
|---|---|---|---|---|---|
| 06-pipeline-orchestration | Task 1 — six-member steps enum and glossary deadline | done | `c8b8a44` | `backend/utils/constants.py` | Full backend suite passes |
| 06-pipeline-orchestration | Task 2 — replace legacy care-plan markers | done | `ac70082` | `backend/utils/markers/markers.py` | Full backend suite passes |
| 06-pipeline-orchestration | Task 3 — remove retired result fields | done | `cd5d2d3` | `backend/models/pipeline_events.py` | Full backend suite passes |
| 06-pipeline-orchestration | Task 4 — wire the four-LLM-call pipeline | done | `eb4d8a9` | `backend/care_plan/pipeline.py` | Full backend suite passes |
| 06-pipeline-orchestration | Task 5 — adapter units, markers, and after-score rendering | done | `5e5297d` | `backend/services/care_plan_pipeline.py` | Full backend suite passes |
| 06-pipeline-orchestration | Task 6 — worker units and provenance stripping | done | `9a13f46` | `backend/routes/worker.py` | Full backend suite passes |
| 06-pipeline-orchestration | Task 7 — terminal stage constant | done | `c92b16a` | `backend/utils/firebase.py` | Full backend suite passes |
| 06-pipeline-orchestration | Task 8 — six frontend processing steps | done | `76f48db` | `frontend/src/components/ProcessingScreen.tsx` | Frontend suite passes |
| 06-pipeline-orchestration | Task 9 — streaming tests | done | `43b262f` | `backend/tests/care_plan/test_pipeline_streaming.py` | Full backend suite passes |
| 06-pipeline-orchestration | Task 10 — executor tests | done | `fcbbd93` | `backend/tests/care_plan/test_pipeline_executors.py` | Full backend suite passes |
| 06-pipeline-orchestration | Task 11 — worker tests and provenance coverage | done | `c367131` | `backend/tests/routes/test_worker.py` | Full backend suite passes |
| 06-pipeline-orchestration | Task 12 — worker end-to-end scenarios | done | `096f423` | `backend/tests/routes/test_jobs_e2e_scenarios.py` | Full backend suite passes |
| 06-pipeline-orchestration | Task 13 — ProcessingScreen tests | done | `b7272ca` | `frontend/src/tests/components/ProcessingScreen.test.tsx` | Frontend suite passes |
| 06-pipeline-orchestration | Task 14 — full-suite verification and run record | done | pending | this artifact | See verification evidence below |

## Verification evidence

- `cd backend && python3 -m pytest tests/ -q` → **741 passed, 5 warnings, 23 subtests passed** in 110.02s; zero failures and zero errors. (`python` is unavailable in this environment, so the repository's documented Python 3 interpreter was used.)
- `cd frontend && npx vitest run` → **20 test files passed, 128 tests passed**.
- `cd backend && python3 -m pytest tests/utils/test_care_plan_markers.py tests/utils/test_firebase.py::test_complete_job_unconditionally_clears_top_level_input_text -q` → **16 passed**. This explicitly covers the marker regression discovered after Task 5.
- `cd frontend && npx vitest run src/tests/hooks/useJobSnapshot.test.ts src/tests/components/ResultScreen.test.tsx` → **2 test files passed, 15 tests passed**.
- The Task 14 smoke import of `CarePlanPipeline`, `run_care_plan_pipeline`, `_strip_internal_provenance`, and `Constants` succeeds, including the assertion that `CORRECT.number == 6`.
- The mandated backend retired-name grep returns zero hits.
- Inspection of `useJobSnapshot.ts` and `ResultScreen.tsx` confirms generic stage/error handling; the mandated `ErrorCode` switch/case grep returns zero hits.
- `git diff --check` passes before the artifact change; the worktree was clean before this artifact was added.

## Deviations from the PRD/TASKS

- Task 5 follow-up — `be97fc9` updates `tests/utils/test_care_plan_markers.py` for the new marker map. This closes the four failures Task 5 exposed but which the original task list did not assign.
- Task 9 — the streaming result assertion uses CarePlan equality and explicitly rejects object identity. Glossary projection returns a copy, so asserting `result.care_plan is CARE_PLAN_FIXTURE` would be incorrect.
- Task 11 follow-up — `5355a3c` preserves the existing raw-field removal while adding internal-provenance stripping. This is intentional defense in depth: completed output removes raw document material and summary/source fact IDs.
- Task 12 — Scenario 9 failure tests patch `_generate_json`, the active structured-output path, rather than the retired text generator. The document-size scenario checks were recalibrated for removal of the former large raw-artifact padding and retain the under-1 MiB and valid-stage assertions.
- Task 14 follow-up — `2372a6a` removes residual retired pipeline identifiers from comments, docstrings, and assertions so the task's literal codebase-wide zero-hit grep is satisfied.

## Follow-ups / new issues discovered

- No unresolved automated failures remain for PRD 06.
- The backend suite emits five non-failing warnings: PyPDF2 deprecation, a Pydantic test-class collection warning, and three Vertex AI SDK deprecation warnings. They are pre-existing dependency/test-framework warnings, not PRD 06 failures.

## Still requires you (from PRD §8 + this run)

- Run the live ngrok + pm2 (`SERVICE_MODE=combined`) smoke test: submit a real note, observe all six stages, verify final rendering, and verify a deliberately corrupted note reaches the clean restart screen.
- During that live run, observe whether the 20-second glossary-curation timeout fires under normal latency; raise the budget if it does.
- Confirm analytics correctly accepts `stage_reached` values 3 through 6 once real traffic flows.
