# Code Run — 2026-09-14 06:50 — 10-numeric-integrity (all 6 tasks + review fixes)
Branch: docs/fidelity-concision-brief   Base: 09aaf3c

## Tasks completed

| Sub-project | Task | Status | Commit | Files | Tests |
|---|---|---|---|---|---|
| 10-numeric-integrity | T1 NUMERACY paragraph | Done | db3c707 | care_plan/prompts/_style_rules.txt | grep checks pass |
| 10-numeric-integrity | T4 NUMERACY prompt tests | Done | a5e8778 | tests/care_plan/test_pipeline_prompts.py | 31 passed |
| 10-numeric-integrity | T2 number tokenizer | Done | f654fed | care_plan/pipeline.py | all AC probes pass; tests/care_plan 183 passed |
| 10-numeric-integrity | T3 _check_numeric_parity wired into _verify_assembly | Done | e86fb72 | care_plan/pipeline.py | 781 passed full suite |
| 10-numeric-integrity | T5 test_pipeline_numeric_parity (new) | Done | d6badef | tests/care_plan/test_pipeline_numeric_parity.py | 26 passed; full 807 passed |
| 10-numeric-integrity | T6 verification | Done | no commit | — | grep/smoke/ruff clean |
| 10-numeric-integrity | Review fixes | Done | 32101e8 | care_plan/pipeline.py + test file | 35 passed; full 816 passed, 23 subtests |

## Deviations from the PRD/TASKS
- Order: T1, T4, T2, T3, T5 (T4 only depends on T1).
- T2: date/time exclusion uses span OVERLAP, not containment — the verbatim PRD snippet left ("30","am") for "7:30 AM", contradicting PRD §7.2's own test intent.
- T5: two supplementary tests added (test_excluded_spans_covers_time_and_date_shapes, test_check_numeric_parity_called_directly_is_log_only) to exercise imported symbols directly.
- Review fixes (commit 32101e8): new `_KNOWN_UNIT_WORDS` closed vocabulary; `_BARE_DATE_RE` now excludes a slash pair unless followed by a known unit (PRD snippet kept any following word, causing false positives on "4/12 with cardiology"); unit-mismatch log prints the unit only if known, else "<non-unit word>" (PRD §4.4's "a unit is generic vocabulary" premise was false for the open regex — drug/person names could be logged); `_UNIT_WORD_RE` now derived from `_UNIT_WORD_MAX_LENGTH` so tuning it takes effect.

## Follow-ups / new issues discovered
- `_KNOWN_UNIT_WORDS` is reasoned, not calibrated — omits e.g. "grams", "liters"; extend if real notes show slash-pair quantities with such units → PRD 16/17 documentation should describe the vocabulary.
- PRD 10 §4.2/§4.4 snippets now differ from code (overlap exclusion, closed unit vocabulary, log placeholder) → PRD 16 technical documentation should document the code as landed.

## Still requires you (PRD §8)
- Prompt smoke test on 5–10 real/realistic notes (ngrok + pm2, SERVICE_MODE=combined): NUMERACY doesn't make the model refuse to render values; check parity log volume for false positives.
- Decide later whether to promote numeric-parity failures into reviewer corrections (out of scope; log-only now).
- Tune `_UNIT_WORD_MAX_LENGTH` / `_KNOWN_UNIT_WORDS` from real log data.
